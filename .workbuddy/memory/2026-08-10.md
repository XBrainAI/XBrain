# 2026-08-10

## 四季景点子站模板提炼（候选）
- 任务：按 6 条需求提炼可复用的【四季景点】子站 HTML 模板，存于 `四季景点/src`。
- 派生来源：惠东周末家庭游（媒体画廊 index.* 主图+缩略图切换、按时间点内嵌图的行程方案）；古埃及展南沙周末家庭游（8段式子站结构重排、多方案 plan-selector 外壳）；XBRAIN-LOGO-IP.md（Logo/风格）。
- 产出 3 个候选（功能等价，组织方式不同）：
  - A 单文件综合蓝本：`四季景点/src/site-template.html`（推荐默认）
  - B 模块化拼装包：`四季景点/src/kit/`（`_01-base.css` / `_02-media-gallery.css` / `_03-itinerary.css` / `_media-gallery.html` / `_itinerary.html` / `_sections-skeleton.html` / `_scripts.js` + README.md）
  - C 主题换肤：`四季景点/src/site-template-themed.html`（data-theme 4 预设：ip-blue/ocean-teal/desert-sand/forest-green，右下角预览切换器）
  - 对比文档：`四季景点/src/CANDIDATES.md`；占位图 `四季景点/src/assets/ph1..6.svg`
- 等待用户选定其一作为正式模板（可能混合，如 A 结构 + C 换肤），选定后写入 README/AGENTS.md 并固定。
- 关键约定：主图命名 `index.*`；导航首条 `← 返回` 链 `../index.html`；换肤仅改 `--accent/--accent2/--hero-grad`（A/B）或 `<html data-theme>`（C）。

## 17:37 补充：候选 B 预览方案
- 用户问「B 模块化怎么预览」。B 是片段包无独立页面，已生成 `四季景点/src/kit/demo.html`：按 README 拼装顺序把 _01/_02/_03.css + 骨架 + 媒体画廊 + 行程 + 脚本缝成一个完整可运行页，图片路径改为 `../assets/ph*.svg`，浏览器直接打开即见全部交互（缩略图切换/方案切换/滚动高亮/移动端汉堡）。
- 同步在 kit/README.md 顶部补「如何预览效果」说明。

## 17:50 选定 A 并固化为正式模板
- 用户确认选 A。将 `四季景点/src/site-template.html` 定为正式模板：去掉残留的 `data-theme="ip-blue"`（A 非换肤版），页头注释改标「正式模板·2026-08-10 选定」，footer 改 `template/official · v1.0`。
- 新增 `四季景点/src/README.md`（模板 hub：文件导航 + 用法 + 硬规则速查 + 预览）。
- 在 `四季景点/AGENTS.md` 末尾新增 §14「标准子站模板（site-template.html）」：位置/组成、已内置 8 段式结构、不替代的 §12/§13 硬规则、换肤与既有子站不回溯迁移。
- `CANDIDATES.md` 顶部加选型结论（选 A）。
- 注：kit/(B) 与 site-template-themed.html(C) 保留为可选变体，不删除。

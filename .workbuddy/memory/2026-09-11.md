# 2026-09-11

## 健康/妈 子站：generate_index.py 支持「孤儿图片」条目

- 背景：用户新增 `20260911-妇科超声-省妇幼.png` 但无同名 MD，问是否能自动上站。原脚本只 `glob("*.md")`，图片仅为同名附件 → 孤图不会显示。
- 改动：`健康/妈/generate_index.py`
  - 新增 `IMG_EXTS = (".png",".jpg",".jpeg")` + `find_image(stem)`，配对不再写死 `.png`。
  - `parse_filename` 改用 `Path(filename).stem`，兼容图片后缀。
  - main() 增加孤儿图扫描：无同名 MD 的图片生成 `img_only=True` 条目，按 stem 与 MD 条目合并倒序排序。
  - build_html：`img_only` 条目图片默认展开、标题加 `.img-only-badge`（橙）、无「查看完整报告」按钮、空 actions 容器不输出；新增 `.img-only-badge` / `.img-only-note` CSS。
- 结果：reports 45 MD + 1 孤儿图 = 46；新条目排时间线首位（2026-09-11）。
- 同步更新 `健康/AGENTS.md` §3.2 / §8.1 / §9（明确无自动构建，必须手动跑脚本 + git push）。

## 健康/妈 子站：孤儿图工单机制（第二轮迭代）

- 需求修正：用户明确「不该等用户手写 MD，而应由 AI 按现有 MD 格式直接生成」，目标是"丢图即上站"。
- 改动：
  - 新增 `orphans.json` 工单：脚本扫描无同名 MD 的图片，写入 `image`/`target_md`/`date`/`project`/`hospital`，并在 stdout 打印 `[TODO]` 清单；每次运行覆盖写入，count=0 即全部就绪。
  - 「仅图片」降级条目文案改为"解读报告生成中（详见 orphans.json 工单）"，作为兜底而非终态。
  - `健康/妈/README.md` 改为推荐「只放图片」流程（6 步）。
- 本轮实际产出：读 `20260911-妇科超声-省妇幼.png`（省妇幼·经阴道彩超，子宫 44×41×45mm、内膜 5mm 内回声欠均匀、双附件未见包块、临床拟诊子宫内膜息肉？），按六段式生成 `20260911-妇科超声-省妇幼.md`（含与 2026-05-29 的四列对比表）。
- 结果：`Total reports: 46 (md: 46, image-only: 0)`，新条目为常规态（查看图片 + 查看完整报告）。
- 沉淀技能：`~/.workbuddy/skills/health-mom-report-intake/SKILL.md`（孤儿图→解读报告全流程 + 妈六段式 MD 规范 + 哥子站差异）。

## 关键认知（易踩坑）
- 妈/哥子站 index.html 由本地 Python 脚本生成，**Netlify build 只编排 query-system**，纯静态子站无 watcher/hook，必须手动 `python generate_index.py` 再 push。
- 妈子站的"自动化"上限 = 脚本负责检测与工单化，**MD 生成必须由 AI 读图完成**（脚本无 AI 能力，也不该引入 API key 依赖）。
- `orphans.json` 已加入根 `.gitignore`（本地运行态文件，每次运行覆盖，不入库）。
- 截至 2026-09-11 收工：20260911 妇科超声条目本地已就绪（46 条 / image-only 0 / 常规态），**用户选择暂不 commit/push**，线上 Netlify 未更新。

## 深度汇总报告同步（用户指出的漏项）

- 问题：新增 20260911 后，`个人健康档案与深度医学分析报告.md` 仍写「44份 / 2026年5月 / 42岁」，与页面时间线矛盾。
- 已增量更新该文件：覆盖时间→2026年9月（跨度4年3个月）；报告总数 44→46；时间线标题 44→46 份；第三阶段改为「2026年5月-9月」并加 2026-09-11 行；妇科涉及报告 8→9 份（妇科超声×3）；内膜息肉段落加 3 行对比表（13mm→6mm→5mm）；异常跟踪表增 2026-09 列 + 子宫内膜行；检查日历过期项标「尽快（原定X月，已超期）」；年龄 42→43 岁；文末 44→46 份。
- 重跑脚本后汇总内容已注入 index.html 顶部（grep 到「46份」「跨度4年3个月」「仍无息肉样占位」）。
- 校验法沉淀进 skill：`grep -n "44\s*份\|42岁\|4年来"` 命中即过期残留。**以后新增子报告必须同步汇总报告**（§4.1 九项清单）。

# 2026-08-24

## 生活点滴 0823「暑假唱K」记录页精修

- **需求**：围绕 `生活点滴/2026/0823/README.MD` + 媒体文件名制定叙事框架，扩展文案，再有机融合图片视频。
- **叙事线**：原计划西樵山石燕岩家庭游因天气改期 → 地铁四手对比（军训后沙手黑过爸）→ 海珠广场堂会等位 → 沙开嗓（妈点歌、调侃爸"你菜啊"）→ 弟接力投入唱 → 自助餐补给 → 唱到 19:04 散场兄弟合影 → 散场小记。
- **媒体处理**：3 个 `.heic` 无法在浏览器显示，用 `pillow-heif` 转 `.jpg`（最长边 1600、q82）并存回同目录；原 `index.html.jpg`（误命名 11MB 原图）复制为 `index.jpg` 作 hero 封面，删除冗余 `index.html.jpg`。
- **页面手法**：未用通用生成器（其默认「高一军训」主题与文案错位），改为手搓精修 `index.html`——复用 `site-template-travelogue.html` 冷色皮肤与全套 JS（媒体画廊/灯箱/导航/Logo 淡出），按时间拆 6 个 `.log-chapter`、视频 inline、新增 `#afterword` 章节与 nav 链接。校验：无 `{{` 残留、section 4/4 平衡、div 50/50、锚点无缺失、`node --check` 通过。
- **画廊**：`gen_life_record.py gallery` 重建（8 卡）；0823 卡片手搓标题「暑假唱K · 2026-08-23」与简介，靠 `extract_existing_titles` 在重跑时保留。README.MD 补「叙事框架」段使底稿与页面一致。
- **提交状态**：本地已 commit（f143d0f）。⚠️ `home` 分支与 `origin/home` 无公共祖先（远端 62 个 commit 含无关内容如"增高速服务器时间/onychomycosis"），属分支引用碰撞——**未强制推送**，待用户确认远端意图后再决定 push 策略。

## 0823 页面两处失真修正（用户反馈）
- **章2「15:20 到了，先等厢」**：原先混入了 `唱K-海珠广场堂会-走-兄弟合影-IMG_20260823_190419.jpg`（19:04）与 `index.jpg`（开唱后现场），时序错位。已移除，仅保留 `等-202608231520.jpg` 一张。
- **章3「沙先开嗓」**：原写「厢一开沙先拿麦、一首接一首」失真。事实：① 15:30 入场即开唱，照片/视频到 17:36 才记录；② 全程断断续续、谁想唱接过去哼，并非连轴。已改时间标签为「15:30 入场 · 17:36 留影」，并重写文案。章4「弟接力」的「一首接一首」同步改为「挑了几首慢慢唱」。README.MD 同步修正。
- 不执行 git 操作（用户要求）。

## 环境提示（可复用）
- 本机无 ImageMagick/ffmpeg；HEIC 解码用系统 Python 3.10 + `pillow-heif`（已 `pip install`）。Managed Python 3.13 缺 PIL。
- `生活点滴/2026/0823/` 两级深目录，跨年媒体引用须 `../../<YYYY>/`。

## 经验回流 AGENTS.md（§8.3）
将本次 0823 作业的 5 条可复用经验写入根 `AGENTS.md` §8.3 生活点滴子站段（未动 git）：
- 画廊手搓标题保留机制（`extract_existing_titles` 重跑安全）；
- 手搓精修页清单追加 `2026/0823`（通用生成器默认「高一军训」主题错位、单 blob 不分章）；
- HEIC 处理规范（系统 Py3.10 + pillow-heif 转 jpg，勿用生成器误重命名 hero）；
- **媒体时序铁律**（按文件名时间戳归属章节，禁止跨时段串图，反例即 0823 章2 误插 19:04 合影）；
- **文案忠实铁律**（不虚构时段起点、不夸大连续性，反例即 0823 章3「厢一开」/「一首接一首」）；
- git 推送纪律（分支无公共祖先=引用碰撞，禁止强推，待用户确认）。

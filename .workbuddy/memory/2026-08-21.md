# 2026-08-21 工作日志

## 家庭自驾出游计划系列（XBrain home 仓库）
用户持续在 home 仓库攒"广州西村出发·周六自驾一日往返"的家庭出行计划，对象含老人+小孩，强调不爬山/不累。已生成：桂山岛、惠东周末、清远佛冈（上岳古村+观音山）、西樵山。

- 今日更新《西樵山自驾一日游计划.md》：用户确认必玩石燕岩乘船。
- 核实关键事实（2026-08）：石燕岩游船试运营中、¥35/人（门市¥68）、包船¥200/9人、9:00-17:00、洞内18℃、需救生衣安全帽、无身高限制大小同价；官方旅游车 B/D 线设「石燕岩站」，南门→亦有亭换乘→石燕岩站→步行600m平路到洞口。
- 西樵山免大门票（公众号预约），旅游车通票¥40（老人/1.2-1.5m儿童半价）。
- 模式：出行计划默认产出 Markdown 源稿存仓库根，不主动转 DOCX/PDF（见用户 MEMORY 偏好）。

## 方案范围确认（09:10）
- 用户明确：出游计划**只考虑西樵山+石燕岩，排除清远佛冈**。已将《西樵山自驾一日游计划.md》定为单一确定路线（去掉备选分支），并补充山脚听音湖为可加项。佛冈计划（清远佛冈自驾一日游计划.md）保留但不在本次范围内。

## 佛山西樵山石燕岩子站编制（10:12）
- 基于 `home/src/site-template.html` 攻略型模板 + `四季景点/AGENTS.md` 规范，编制 `四季景点/佛山西樵山石燕岩周末家庭游/index.html`（单方案 planA 必乘船·不爬山版，约 600 行）。
- 8 段结构齐全：hero→overview→spots→plans→culture(讲给孩子听)→hours→hotels→weather→tips。绿石主题（`--accent:#5fb98a` 玉绿）。
- 已遵守 §7 对比度（`--text/#eef2f8` 不透明）、§13 攻略强制规则（营业时间+人文+天气评估+深链+返回顶部+入屏动画）、§14 模板用法、§15 SITES 模型。
- 天气重点：8/22（周六）正处南海区 21–22 日中到大雨局部暴雨过程，石燕岩游船遇雷雨大风停运风险🔴高，已给保底方案（室内段南海博物馆+黄飞鸿馆或改期）。
- 导航注册：SITES 数组追加条目（`status:'published'`、`updated:'2026-08-21'`），封面随后接线为目录内已有的 `index.html.png`（石燕岩"水底牌坊"实拍，1.5MB），`cover: ''` → `'佛山西樵山石燕岩周末家庭游/index.html.png'`。
- 校验通过：DIV 133/133、SECTION 9/9、EMPTY td=0、anchor 全部命中、内嵌 JS `node --check` OK、导航 SITES 语法 OK。
- 仓库根 Markdown 源稿 `西樵山自驾一日游计划.md` 与子站为"源稿→展示页"关系，子站描述已注明"规划页（尚未执行，无实拍）"（注：封面用的是景点代表照，非本人实拍）。

## 封面不显示排查（10:34）— 结论：运行时/部署问题，非架构问题
- 现象：导航卡片封面（`index.html.png`）未显示。逐项核查：渲染标记§15.4 `coverHtml` 路径写法正确、与同页"河源"等条目同构；文件合法 PNG(1080×1378)；CSS `.site-card-image` height190+cover 正常；本地 `python -m http.server` 实测该中文路径图 200 + `image/png` + 1.51MB（与对照一致）。
- **根因：该 `index.html.png`（及整个子站目录）从未 `git add`，`git ls-files` 报 not known to git → Netlify 部署后 404 → 卡片按 §15.4 第979行"背景图加载失败不触发 onerror、原 fallback 是死代码"静默空白。**
- 已 `git add 四季景点/佛山西樵山石燕岩周末家庭游`（含 index.html、封面及用户已放入的实拍子目录 石燕岩/南海观音寺/黄飞鸿馆/听音湖/）。待用户 commit+push 生效。
- 复用结论：子站封面/资源图只要没进 git index，线上必 404 且无任何报错——排查"图不显示"先 `git ls-files --error-unmatch <path>` 确认是否入库，比改标记快。
- 附带发现：该子站目录下已有用户实拍图（石燕岩/index.png、南海观音寺/、黄飞鸿馆/、听音湖/ 等），说明出行已发生；当前子站页是"规划页"架构（无 media 画廊）。若要严格"按规范显示图片"，应改用 travelogue 模板补建 hero+`.media-block` 画廊（§14.6）。

## 子站卡片不显示景点图（10:50）— 架构缺件，已修复
- 用户指：子站目录内各景点文件夹图片没显示在子站相应卡片内。核查：攻略模板 `home/src/site-template.html` 每张 `spot-card` 本就内嵌 `.media-block`（主图+缩略图，行 447-463、CSS 176-187、JS 842-873 自动选 index.* 为首图+缩略图切换）；我建页时按"规划页"把媒体组件整体删了 → 卡片无 `<img>`，故空白。
- 磁盘真实图片（经 git status + git add 证实，沙箱 os.listdir 偶有状态不一致假象）：`石燕岩/`（6 张：index+image+image copy 1-4）、`南海观音寺/`（1 张 index.png）、`黄飞鸿馆/`（index.png+image.png）、`听音湖/`（index.png+image.png+image copy.png）。4 个文件夹本机均存在。
- 修复：① 把模板 `.media-block` CSS（含 `.img-portrait` 竖屏 `object-fit:contain`）移植进子站页；② 植入媒体 JS（封面自动选 index.* + 缩略图切换 + 自补 `syncOrient` 竖屏适配，攻略模板本身无 syncOrient）；③ 给 4 张景点卡 `spot-shiyan`(6图)/`spot-guanyin`(1图)/`spot-huangfeihong`(2图)/`spot-tingyin`(3图) 全接 `.media-block`。校验：DIV 145/145、media-block=4、<img>=16、JS `node --check` OK。
- **坑：文件名含空格 `image copy.png`**。HTML `src` 写 `%20` 会被浏览器二次编码成 `%2520`→404；正确写法是 `src` 里保留**原始空格**，浏览器自行编码为 `%20`→服务端解码命中。已改回裸空格。
- 校验：本地 HTTP 实测 `石燕岩/index.png`、`南海观音寺/index.png`、`石燕岩/image copy.png` 均 200 image/png；子站页内嵌 JS `node --check` OK；DIV 139/139、`.media-block`=2、`<img>`=9。
- 注意：子站整目录此前 `git add` 过（含 石燕岩/ 南海观音寺/ 图），但仍未 commit+push；要线上显示须 commit+push（纯静态无需 build）。

## 已 commit + push 上线（11:xx）
- `git commit` + `git push origin home` 完成（commit c29d6ba，16 files changed, 815 insertions）。Netlify 自动部署纯静态子站，无需 build。
- 推送前复核：子站 DIV 145/145、`.media-block`=4、`<img>`=16、无 `%20` 二次编码残留；导航 `cover: '佛山西樵山石燕岩周末家庭游/index.html.png'` 已接线确认。
- **现状：封面与 4 张景点卡实拍图现已线上可见**。导航 SITES 条 `desc` 仍写"规划页（尚未执行，无实拍）"——与现状不符（已有实拍），待用户确认是否更新描述。

## 图片二次修复 + 行程卡片同步显示（11:12）
- 用户新要求：① 黄飞鸿狮艺馆图不显示→修；② 听音湖无图→HTML 不显示；③ 观光车/西樵山南门停车楼（用户补充的参考卡）补卡片+图；④ 行程方案(plans)时间线卡片同步显示图。
- **根因（黄飞鸿 404）：磁盘文件夹被改名 `黄飞鸿馆/`→`黄飞鸿狮艺馆/`（且新增 image copy.png 共 3 图），HTML 仍指向旧名 `黄飞鸿馆/`→404。** 已改指向新文件夹。
- **磁盘现实（os.walk 权威）**：石燕岩(6:含 image1.png，注意 `image copy.png` 已被用户改名成 `image1.png`)、南海观音寺(1)、黄飞鸿狮艺馆(3)、观光车(2)、西樵山南门停车楼(2，原磁盘名"西焦山南门停车楼"错别字→改对 `西樵山南门停车楼`)、听音湖(无)。
- **沙箱 git 坑（重要）**：本环境 Bash 跨命令文件系统/索引快照不一致。现象：`git add -A` 单命令里 `黄飞鸿狮艺馆/`、`观光车/` 一度"消失"、且 `石燕岩/image copy.png` 在快照中变成 `image1.png`。→ 经验：**不能只信某次 os.walk 或某次 git status**，必须以 `git ls-tree -r HEAD`（core.quotepath=false 解码）为部署树权威，逐一核对 HTML `src` 是否命中；发现 `石燕岩/image copy.png` 不在部署树（实为 image1.png）后改 HTML 对齐。
- 改动：5 张 spot 卡（石燕岩/南海观音/黄飞鸿/观光车/停车楼）含 media-block；听音湖去媒体块；plans 时间线 5 节点（南门/接驳/石燕岩/观音/黄飞鸿）补 media-block 同步显示。DIV 173/173、media-block=10、img=38、无 %20。
- 提交链：c29d6ba(初修) → 7911f1f(全面接线，但石燕岩 仍引 image copy.png) → 6edf977(对齐 image1.png + 清听音湖幽灵)。已 push origin/home，Netlify 自动部署。
- **结论**：所有 HTML 引用现已 100% 命中部署树（缺失 0）。提示用户：石燕岩 文件夹内图片现为 `image1.png`（非 `image copy.png`），如需保持"image copy"命名规范可自行改名后我重接。

## 出发时间改 07:15 + 新增西樵镇午餐老字号章节（11:31）
- 用户要求：出发时间改为 07:15；补充【西樵镇午餐】推荐餐厅（地道/老字号优先）。
- 出发时间：行程表(到达顺调08:15)/时间线/雨天预案三处 07:30→07:15，下游时间不动（南门多 15min 缓冲，更稳）。下游"8 点前出发最稳"贴士仍一致。
- 新增独立章节 `id="lunch"`（置于 plans 之后、culture 之前），导航桌面+移动双份加 `#lunch` 锚点；行程时间线午餐节点加 `#lunch` 跳转。
- 午餐章节内容（基于联网检索实据，非杜撰）：6 张 spot-card 精选——宏八酒楼(西樵山景区店,下山首选本地粤菜)、双溪公馆(1893百年·听音湖畔)、旺阁渔村(1993老字号·粤菜海鲜)、君临点心专家(佛山老字号·全天茶市)、观山美厨(河鲜·央视打卡)、龙辉小食店(1994煲仔饭)；末尾 info-card 补充江浦西路市井老字号(聚福堂/便群小食店/阿凤小吃)。卡为纯文本(无图，用户未提供餐厅图)。
- 校验：DIV 206/206、07:30残留0/07:15出现3、nav #lunch=3、午餐 .spot-card=6、纯文本无悬空 img、所有 src 命中部署树无404。
- 提交：74e14d7 → push origin/home，Netlify 自动部署。纯静态无需 build。

## 用户疑惑"子站图片没入 git"（11:42）
- 用户反馈：子站目录下文件都要入 git，但待提交列表没显示很多图片。
- **核查结论：图片其实全在 git 里**。`git ls-tree -r HEAD` 与 `git ls-tree origin/home` 抽验——子站 16 文件（含全部图片）均已在 HEAD 且已推 origin；未跟踪 0、幽灵 0、磁盘与 HEAD 完全一致。
- **为何 status 不显示**：`git status` 只列相对 HEAD 有变化的文件；图片早已在前序提交(c29d6ba/6edf977/74e14d7)入库，磁盘=HEAD 故状态干净，这属正常非漏提。
- **真问题暴露**：导航首页 `四季景点/index.html` 相对 HEAD 多出 SITES 子站条目（封面接线）——说明首轮 commit c29d6ba 在沙箱里没把导航文件真正带进库，导致线上导航卡片封面一直未生效。已补回并提交推送（916c2b2）；同时将 desc 由"规划页（尚未执行，无实拍）"更正为"含石燕岩水底牌坊实拍与西樵镇老字号午餐推荐"。
- **经验固化**：排查"图片没入 git"先 `git ls-tree -r origin/<branch> <dir>` 看远端是否已有（而非看 `git status`），能立刻区分"已入库/真未跟踪"。本仓库静态资源只要进了 HEAD 即随 push 上线，无需 build。

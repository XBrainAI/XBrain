# 2026-08-11

## 四季景点子站模板：新增「纯游记」模板
- 新增 `四季景点/src/site-template-travelogue.html`：纯游记/记录型模板（暖色皮肤默认 `--accent:#ffc46b`）。
- 复用官方攻略型模板（site-template.html）同一套 XBrain IP Logo、top-nav、媒体画廊（`.media-block` 主图+缩略图切换）、JS（画廊/滚动高亮/汉堡/Logo淡出/入场动画）。
- 结构（轻量，事前不规划）：hero(meta:日期/同行/交通/天气) → 行程总览(事实卡) → 游记实录(`.log-chapter` 章节+组图) → 美食&随手发现 → 实用信息。
- 与攻略型区别：无多方案切换、无强制营业时间复核、无出行前天气预警；营业时间/花费属回忆性备注。
- 同步清理上轮遗留：README.md 与 AGENTS.md §14 已移除已删除的 `kit/`(B)、`site-template-themed.html`(C)、`CANDIDATES.md` 引用，并补两套模板「何时用哪个」说明。
- 验收：div 40/40 配平、assets/ph*.svg 全部存在、导航锚点无失效。

## 首图自动解析（index.* 优先 + 缺失回退）—— 两个模板同步
- 需求：画廊图片文件夹中若有 `index.*` 则作主图；漏命名则回退第一张；兼容缺失不空白。
- 实现：媒体画廊改为「画廊为唯一数据源」，JS 自动解析首图——`baseNameOf` 取文件名去扩展名判等 `index`（大小写/扩展名/路径无关，`a.b.index.svg` 不会误判）；`isIndexImg` 优先，否则 `thumbs[0]`；当前主图 `.is-cover{display:none}` 从缩略图条隐藏，点击切换时原主图回到条中；仅一张图时隐藏整条。
- 在 `四季景点/src/assets/` 新增 `index.svg` 占位演示图，并在两模板首个媒体块画廊首位引用，实时演示 index.* 优先。
- HTML 里 `<img class="media-cover">` 的 src 现为占位、会被 JS 覆盖；定首图须在画廊命名 `index.*` 或调顺序（不要手改封面 src）。
- 验证：Node + 最小 DOM mock 跑通首图解析算法，14/14 断言通过（index优先、缺失回退、点击切换恢复、单图隐藏、index在中间仍优先）。
- 文档同步：README.md 新增「首图自动解析」通用规则段；AGENTS.md §14.1/§14.2 补同规则；清理 site-template.html 头部残留的 kit/CANDIDATES 失效引用。

## src/ 最终清理（仅保留 2 模板）
- 用户确认「只需 2 个模板」：攻略型 `site-template.html` + 纯游记型 `site-template-travelogue.html`。
- 实际执行删除（上一轮删除命令曾被用户中断未生效）：`kit/`(候选B)、`site-template-themed.html`(候选C)、`CANDIDATES.md`。
- 保留必要支撑文件：`README.md`(模板 hub 说明)、`assets/`(ph1..6.svg + index.svg 占位图，两模板预览引用)。
- 校验：删除后 src 顶层仅剩 4 项；AGENTS.md §14、README.md 第4行原有「已清理」声明现在真正成立，无悬空功能引用。

## 四季景点主站入口页重设计（数据驱动）
- 动因：子站增多（9 个：7 已发布 + 2 草稿），原静态 7 卡片网格不便浏览/管理。
- 重写 `四季景点/index.html`：页面内 `SITES` JS 数组为唯一数据源（9 子站元数据：name/title/region/regionTag/theme/type/status/updated/cover/desc），渲染 双视图（卡片浏览 / 管理表格）+ 统计条 + 搜索 + 状态/地区/主题筛选(动态带计数) + 排序 + 草稿专区(「用纯游记模板补建」入口) + 模板入口面板。
- 草稿子站（白云乐8小城、老表大岗）已有实拍素材无 index.html → 显示「草稿·待建」徽标并指向纯游记模板；封面 `onerror` 自动回退渐变。
- 9 个封面图路径全部核验存在；div 30/30 配平；JS `node --check` 通过。
- 维护：加子站=在 SITES 数组追加一条对象，统计/筛选/管理表自动同步。
- 文档：新增 AGENTS.md §15（入口页数据驱动说明）+ 修正 §14.4「手动加 .site-card」为「SITES 数组追加」。
- XBrain IP 一致：沿用 index.html 内联 Logo 与深色主题变量。
- 后续：在 §15 双视图基础上新增**时间轴视图**（工具栏「时间轴」按钮）：竖向时间线按 `updated` 升序串子站，每节点含日期刻度+状态点+封面缩略图+标题/标签/简介+入口，同日期合并日期标签；复用 SITES/筛选。CSS 加 `.timeline-section/.tl-item/.tl-date/.tl-marker/.tl-card/.tl-thumb`；入场动画重构为通用 `revealItems(container)`（原 observeCards 改名）。div 44/44、JS 通过、逻辑测通过。AGENTS.md §15.1 同步补充。
- **按用户要求整改**：时间轴不单独占视图开关，改为**内嵌进「卡片」视图**（卡片网格下方，随卡片一起显隐）。移除工具栏「时间轴」按钮、`currentView==='timeline'` 分支与 `timelineSection` JS 引用；将 `#timelineSection` 区块移入 `.sites-section` 内部并加「出行轨迹 · 时间轴」标题块；`render()` 在卡片视图下同时 `revealItems(grid)` 与 `revealItems(timelineBody)`。校验：div 47/47、JS 通过、无 isTimeline 残留、时间轴确在 sites-section 内。AGENTS.md §15 同步改为双视图+内嵌时间轴说明。

- 修正 四季景点/index.html 时间轴：改为标准「垂直时间轴 + 节点卡片」——左侧一条垂直主轴，每个节点一个发光圆点骑在轴上，圆点右侧挂完整卡片（日期徽标 + 状态标签 + 缩略图 + 标题 + 描述）。移除了移动端把轴线和圆点 display:none 退化为列表的逻辑，移动端也保留垂直时间轴与节点；并支持窄屏卡片纵向排列。JS 相应重构 timelineHtml/renderTimeline（日期改为卡片内 tl-date-badge，去掉旧的左侧 tl-date 列与 hideDate 合并）。已通过 div 配平与 node --check。

- 再次修正 四季景点/index.html 时间轴：用户要求对齐【古埃及】子站「行程表后跟行程展开时间轴」模式。改为：顶部一张行程概览表（日期/子站/类型/状态，对标 plan-summary-table），其下按整理日期分组（DAY 分隔符 tl-day-divider/tl-day-badge），每个节点用竖线串联（.tl-node 的 border-left + .tl-dot 圆点骑线，对标 timeline-item/timeline-dot），节点内容为「时间+标题+描述+标签」块，已发布整块可点跳转。移除了上一版「左侧主轴+缩略图卡片」结构。revealItems 改为 container.querySelectorAll('.site-card, .tl-node') 以正确触发节点入场动画。已通过 div 配平 (49/49) 与 node --check。

- 改造 四季景点/index.html 卡片视图为「垂直时间线信息流（朋友圈风）」：把原来「网格卡片 + 独立时间轴区块」双形态合并。做法：.sites-grid 改为 block 单列窄容器(max-width:720px 居中)；每张卡片包裹进 .site-card-wrap（border-left 竖线 + padding-left + 发光 .tl-dot 圆点骑线），卡片顶部加 .tl-date-badge「整理于 YYYY-MM-DD」日期徽标；移除了独立的 .timeline-section（概览表+展开节点）区块及其废弃函数 tlThumb/timelineNode/renderTimeline，revealItems 观察目标改为 .site-card-wrap。卡片 hover 改为仅发光高亮（去掉上浮位移），让卡片紧贴竖线不分离。校验：静态HTML div 23/23、动态卡片渲染 54/54 配平，node --check 通过，无旧时间轴类残留。

- 重做 四季景点/index.html 时间轴的设计分层（按用户三项决策）：①主轴改用出行/执行日期 `date`（占位阶段回退 updated，已为每条注入 date 字段；古埃及推断 2026-08-01、乐8小城推断 2026-08-09，其余=updated 占位）；卡片同时保留『出行于 date』徽标 + 『整理于 updated』副标。②规划/未实现子站不进时间轴——时间轴 stream 只渲染 published（已实现出行），草稿仍留在下方既有『草稿/待建子站』面板；筛选仅命中草稿时时间轴区显示 timeline-empty 提示。③date 先用 updated 占位，结构就位待补真实出行日。排序下拉新增 date-asc(默认)/date-desc，applySort 支持 date 维度；管理表『更新』列改为『出行/整理』双行显示。校验：静态 div 23/23、动态已发布卡片 49/49 配平，node --check 通过。

- 按用户三项决策落地 四季景点/index.html 时间轴：①未发布(草稿)子站也进入时间轴——render() 取消『仅已发布』过滤，时间轴渲染整个筛选集；草稿节点用琥珀色 .tl-dot-draft 圆点 + 『待整理』徽标 + 『用纯游记模板补建』CTA（非链接 draft-card）。②出行时间以游记显式提供为准——逐站检索各子站 index.html，确认 7 个已发布均为『攻略』通用方案、无显式出行日；仅 古埃及(游记素材文件夹 20260801-02→2026-08-01)、乐8小城(文件名 2026-08-09) 有显式值，其余 7 条 date 置空并标注『出行日期待补』(琥珀色 tl-date-missing 徽标)，待用户补真实出行日。③排序规则：主轴出行时间(date)，同日/均缺按更新时间(updated)兜底；缺失出行日条目统一沉到时间轴末尾（dated.concat(missing)）。管理表出行列无 date 显示『待补』。校验：静态 div 23/23、动态 9 卡片 63/63 配平，node --check 通过。

- 重构 四季景点/index.html 时间轴排序（按用户新规）：①状态筛选改为强制二选一【已实现】(默认,有游记,=published)/【未实现】(无游记,=draft)，取消「全部」chip；filters.status 默认 'published'，syncChips 对 status 组特判（无 all、标签 已实现/未实现）。②移除手动 #sort 下拉（HTML/els/监听/getFiltered 引用全清，连同 .sort-select 死 CSS），timeline 排序轴改为由状态决定——已实现按出行时间 date 升序(缺失沉末、同日 updated 兜底)，未实现按更新时间 updated 倒序(新→旧)。③文案统一：统计卡 已实现/未实现、cardInner 徽标 已实现/未实现、rowHtml 迷你徽标、草稿面板标题改「未实现 / 待建子站」。校验：静态 div 23/23、node --check rc=0；动态 published(7) 49/49、draft(2) 14/14 配平，排序符合新规。

- 四季景点/index.html 时间轴节点旁新增「具体时间标签」（.tl-time）：紧贴圆点右侧、骑在竖线旁的小胶囊。逻辑：已实现显示出行时间 date（缺失→琥珀色「待补」），未实现显示更新时间 updated（琥珀色）。desktop left:17px/top:18px，mobile left:13px 收窄字号。校验：静态 div 23/23、动态 9 卡 72/72 配平，node --check 通过。

- 修复 四季景点/index.html 时间轴 timetag 与卡片重叠：原 .tl-time 绝对定位 top:18px 与卡片顶部(top:0 起点)同高重叠。改为卡片下移——.site-card-wrap 增加 padding-top(桌面34px/移动28px) push 卡片下沉，圆点(.tl-dot top:5px↘/移动4px)与时间标签(.tl-time top:1px↘/移动0)对齐到顶部形成「时间节点头部」，卡片挂其下方。CSS 仅改，div 37/37 配平、JS 通过。

- 【重要定义澄清】四季景点/index.html 的【已实现】严格定义为「有执行后的游记（以子站出现游记为准）」，不再等同于「有无 index.html 页面」。当前仅「古埃及展+南沙度假」有游记(hasLog)，其余 8 个（6 个有攻略页但无游记 + 2 个草稿无页）均为【未实现】。据此把原 `status`(published/draft，仅表示有无页面→决定链接/CTA 与草稿面板) 与「是否已实现」拆成两个维度：新增 `hasLog` 字段（仅古埃及 true）。getFiltered 状态筛选、renderStats 统计、syncChips chip 计数、cardInner/timelineItem/rowHtml 徽标与节点圆点/时间标签颜色，全部改为以 `hasLog` 为准；`status` 仅保留用于链接卡片 vs 草稿 CTA 及草稿面板。古埃及 type 由「攻略」改为「游记」以匹配事实。校验：括号配平提取 SITES 仿真——已完成1(古埃及@2026-08-01)/未实现8、已实现+未实现=9 无重叠；div 37/37、JS 通过。后续某子站补建真实游记后即置 hasLog:true 自动归入【已实现】。

- 四季景点/index.html 卡片首图统一规则：采用对应子站目录下的 `index.html.<图片扩展名>` 文件；无则显示空白（不做占位图）。9 条 SITES 的 `cover` 全部改为指向各子站根目录的 index.html.*（从化/香港/古埃及/老表=jpg；深圳/河源/惠东=png；花都=index.html.jpeg）；白云乐8小城周末家庭游子站无 index.html.* 文件→`cover:''` 留空。同步简化 `coverHtml`：有 cover 渲染背景图 div，无 cover 渲染 `.site-card-image.empty`(透明空白)；删除原 SVG 渐变占位 fallback（背景图失败本不触发 onerror，原 fallback 为死代码）及其 CSS `.fallback*` 规则，新增 `.site-card-image.empty{background:transparent}`。校验：8 个 cover 路径磁盘均存在、白云乐8 正确留空、div 37/37、JS 通过。

## 规则固化：入口页经验沉淀进 AGENTS.md
- 触发：用户要求将对话中各轮迭代产生的规则经验（可固化但未固化）写入 AGENTS.md。
- 重写 四季景点/AGENTS.md §15（原 §15 仍为早期设计，与现状多处矛盾）：删除「全部/已发布/草稿」「时间轴按 updated 升序」「排序下拉」「缺图 onerror 回退占位」等失效描述，新增并固化当前权威口径——
  - §15.1 页面能力（已实现/未实现 强制二选一、无全部、无排序下拉、由状态驱动）；
  - §15.2 时间轴朋友圈式垂直信息流结构 + `.tl-time` 节点时间标签 + ⚠️`.site-card-wrap` 必须保留 `padding-top` 防 timetag 与卡片重叠（已踩坑）；
  - §15.3 【已实现】严格定义(有执行后游记≠有 index.html 页)、`hasLog` 与 `status` 正交解耦、排序轴由状态决定(已实现按 date 升序/缺失沉末、未实现按 updated 倒序)；
  - §15.4 首图规则(取子站 `index.html.<ext>`，无则留空不占位)；
  - §15.5 SITES 字段模型(name/title/region/regionTag/theme/type/status/updated/date/cover/desc/hasLog，现状 date 仅古埃及/乐8 有值、hasLog 仅古埃及 true)；
  - §15.6 维护要点(加子站/标记已实现/改维度/品牌/封面命名)；
  - §15.7 改动验证(静态div配平 + node --check + 括号配平SITES仿真 + ⚠️heredoc反斜杠陷阱→Write成文件执行)。
- 同步修正 §14.4 引用：删「封面 onerror 回退渐变占位」、补 hasLog/date 字段、改『草稿·待建』为『未实现』徽标、明确无排序下拉。
- 代价：仅文档更新，未改 index.html 代码；本地校验 AGENTS.md 为合法 Markdown（结构完整）。

## 固化「纯游记子站内容生成规则」→ AGENTS.md §14.5
- 触发：用户明确「前面已提前开发 四季景点/src 模板（含纯游记型 site-template-travelogue.html）用于后续规范生成，无需再参考其它游记结构」，要求先固化规则再重建子站。
- 在 四季景点/AGENTS.md §14（标准子站模板）下新增 **§14.5 纯游记子站内容生成规则（素材→页面）**：① 素材即数据源（README.MD 叙事底稿 + 文件名时间戳⇄章节时间锚点 / 中文描述⇄主题关键词）；② 文件名→.log-chapter 映射（按时间戳升序、连续时间簇/同主题聚合、`chapter-time` 取簇最早、视频同簇归入）；③ 叙述还原+真挚情感（禁止规划腔/虚构，营业时间/花费写在实用信息属回忆性备注）；④ 首图/封面（画廊首张=主图，把想作主图的图放画廊第一张；子站 `index.html.<ext>`=导航封面）；⑤ 媒体平铺约定（素材平铺子站根、相对路径引用、特殊字符原样保留）；⑥ 发布联动（建成即把 SITES 对象置 published+hasLog:true+填 date+设 cover 升【已实现】）。
- 同步修正 §15.4 过时描述：白云乐8 子站目录下已有 `index.html.JPG`，故其 `cover` 已设 `./白云乐8小城周末家庭游/index.html.JPG`（此前「仅白云乐8无封面」描述作废）。

## 基于纯游记模板构建「白云乐8小城周末家庭游」子站
- 派生源：`四季景点/src/site-template-travelogue.html`（纯游记模板），按 AGENTS.md §14.5 工作流生成 `四季景点/白云乐8小城周末家庭游/index.html`。
- 素材：本目录 README.MD（叙事底稿：三年前学校发"钱"、三年后带回再用；荒废商场嘉大广场；沙陪玩两项目后因同龄太小退场等候；弟最迷升旗/押运/公安/消防的枪；13:30乐园简餐、14:00结束；转角云门NEWPARK+盒马+晴空进击社拿礼物当场拆；往返见广州设计之都；君荟楼下分餐）+ 76 个带时间戳的图片/视频文件名。
- 结构：hero(2026-08-09 · 爸妈沙弟 · 自驾 · 广州盛夏) → 行程总览(关键事实卡) → 游记实录 6 章（08:09 芳记早餐 / 10:03 乐8初体验·荒废商场 / 11:44 升旗押运公安消防·枪声里的少年心 / 13:00 考古农业匆匆午餐 / 14:30 云门NEWPARK·转角遇见的好商场 / 18:18 归途君荟分餐）→ 美食&随手发现(芳记/盒马妈菜/君荟分餐) → 实用信息(回忆性备注) → footer v20260811-151300。叙述在 README 事实上补充真挚情感（旧币彩蛋、沙的尴尬与陪伴、男孩成长从玩具枪开始、新旧商场对比、家常分餐的踏实）。
- 模板扩展：媒体组件默认只支持图片，**视频(.mp4)单独加 `.chapter-videos` 组 + 配套 CSS**（不可塞进 `.media-gallery`），6 章共嵌入 13 段 mp4（地震模拟/直播间/升旗队/押运/公安/射击/农业/云门×3 等）；画廊首图按"第一张=主图"自动解析（无 index.*）。
- 导航联动：SITES 中白云乐8 置 `status:'published'` + `hasLog:true` + `date:'2026-08-09'` + `cover:'./白云乐8小城周末家庭游/index.html.JPG'` → 从【未实现】升【已实现】，已实现现共 2（古埃及+乐8）。
- 校验：子站静态 div 70/70 配平、内嵌 JS node --check 通过、74 个本地媒体引用全部磁盘存在（修复 1 处误引 `云门NEWPARK-2026-08-09-143113.JPG`→实为 `.mp4` 已在视频组）、SITES 仿真已实现2+未实现7=9 无重叠。临时校验脚本 `四季景点/_validate.js` 已清理。

## 基于纯游记模板构建「老表大岗周末家庭游」子站
- 派生源：`四季景点/src/site-template-travelogue.html`（纯游记模板），按 AGENTS.md §14.5 工作流生成 `四季景点/老表大岗周末家庭游/index.html`（此前该条目为 draft、desc 标"素材已备待记录"）。
- 素材：本目录 README.MD（叙事底稿：爸取螺丝刀换713空调滤芯→太晒改二沙岛换+整车吸尘；加油站加燃油添加剂清积碳、需高速高转→借东新高速回大岗顺路完成；近12点到大岗老表一家也到，老表=兄弟妈妈表妹之子、顺德两兄弟；家小挤、搬桌进空调客厅、妈盒马熟食加餐、孩子一桌大人一桌；17点潭州金悦海鲜酒楼意外好吃；归途过妈念叨的北流小吃街没下车；妈说"下次跟老表相聚是否高考后"→珍惜眼前）+ 21 张带时间戳图片（无视频）。
- 结构：hero(2026-08-08 · 全家+老表两家 · 自驾713 · 盛夏酷暑) → 行程总览 → 游记实录 5 章（07:00 二沙岛换滤芯 / 07:20 加油站加剂 / 12:00 大岗团聚 / 17:10 潭州金悦晚饭 / 19:15 北流小吃街与妈的话）→ 美食&随手发现(盒马熟食/潭州金悦/北流小吃街留待下次) → 实用信息(回忆性备注) → footer。叙述在 README 事实上补充真挚情感（爸的养护之爱、出发与保养在加油站连成一事、亲上加亲的缘分、家常团聚不讲究排场、妈那句"高考后"勾勒孩子长大与相聚可贵）。图片按时间戳升序、连续时间簇聚合入章；画廊首图自动取每章第一张（无 index.*）。
- 导航联动：SITES 中老表大岗置 `status:'published'` + `hasLog:true` + `date:'2026-08-08'` + `cover:'老表大岗周末家庭游/index.html.jpg'`（子站根已有该首图）+ 删 `isDraft`、title 改'老表大岗家庭游'、theme 改'家族探亲'、updated 改 2026-08-11 → 从【未实现】升【已实现】，已实现现共 3（古埃及@0801 + 乐8@0809 + 老表大岗@0808，按 date 升序重排）。
- 校验：子站静态 div 60/60 配平、内嵌 JS node --check 通过、25 个 img 引用全部磁盘存在、导航 div 37/37 / JS 通过、SITES 仿真已实现3+未实现6=9 无重叠。临时校验脚本 `四季景点/_validate.js` 已清理。

## 四季景点导航 timeline 改为时间倒序
- 触发：用户要求主入口 timeline「按时间倒序排列」。
- 改动 `四季景点/index.html` 的 `getFiltered()`【已实现】分支（此前为出行日 date 升序）：比较翻转 `(da>db?-1:da<db?1:0)` + 同日 updated 兜底也改倒序，注释「出行时间倒序（晚→早），最新在最上层」。缺失出行日仍 `dated.concat(missing)` 沉末尾（最旧端）。【未实现】分支本就 updated 倒序，不变。现状已实现 3 个倒序为 乐8@0809 → 老表大岗@0808 → 古埃及@0801。
- 文档同步：AGENTS.md §15.3「已实现按 date 升序」改为「按 date 倒序（由近及远，最新在最上层）、updated 倒序兜底」。
- 校验：静态 div 37/37 配平、内嵌 JS node --check 通过、括号配平提取 SITES 仿真已实现倒序正确（3 项）、已实现3+未实现6=9 无重叠。临时校验脚本已清理。

## 按攻略模板更新「从化吕田狮象岩」子站
- 触发：用户要求「按 src 的相关模板，更新从化吕田狮象岩子站」。从化是**攻略/科普**型（素材为公开小红书探洞记录 9 张中文名图 + PTitle hero 图 + index.html.jpg 导航封面，无时间戳实拍、无 README、无视频），故用**攻略型模板** `src/site-template.html`（而非纯游记模板）。
- 重写 `四季景点/从化吕田狮象岩/index.html`：沿用模板骨架（深色基底 + top-nav 固定章节导航 + .media-block 媒体组件 + 统一 JS：媒体画廊首图自动解析/导航滚动高亮/汉堡/Logo 淡出/入场动画），换肤为**自然绿**（--accent #6db33f / --accent2 #a4d65e / hero-grad 绿系，--xb-mid/light 偏深绿）。hero 保留 PTitle.jpg 背景（新增 .hero-clip/.hero-bg/.hero-overlay，hover 缓慢缩放）。
- 章节重组（保留原页全部真实内容）：概览(#overview，含 3 info-card + 1 media-block) → 地理·地质(#geo，岩体特征 data-table + 媒体) → 考古·历史(#history，culture-card + 考古大事记 timeline 5 节点 + 白庙沟/近现代) → 探洞·秘境(#cave，洞内奇观 + 6 图大 media-block + 探洞安全 tips-box) → 四季风光(#seasons，4 info-card) → 游览指南(#guide，基本资讯/交通/推荐路线 data-table + 装备 tips-box) → 今日狮象村(#village，乡村新貌 data-table)。footer 版本 v20260811-155300，标注"攻略科普·素材源自公开小红书探洞记录"。
- **修正历史问题**：①补上缺失的 top-nav 顶部章节导航（原页没有，违反 AGENTS.md §3 长文页规范）；②删除原外链 `http://www.conghua.gov.cn/...` 航拍图（不符合素材平铺约定，现页面 0 外链）；③原手写 fade-in JS 统一为模板 JS；④Logo href 由 #top 改 ../index.html 返回导航。
- 图片映射：9 张中文名图全部入 media-block（哈迪斯_17 概览主图 / 走访_3 地理 / 走访_2 考古 / 哈迪斯_4+3+6+17+探洞_7+10 探洞 / 走访_5 四季），带空格文件名（走访吕田狮象村_ 探访…）在 src 用 %20 编码。
- 导航联动：**SITES 无需改动**——从化仍是 status:'published'（有页）+ 攻略型（无 hasLog，属【未实现】）+ cover 已指向 index.html.jpg。
- 校验：静态 div 100/100、section 8/8 配平、内嵌 JS node --check 通过、23 个 img 引用磁盘全部存在（%20 解码后）、外链 0。临时校验脚本已清理。

## 按攻略模板重建「香港周末家庭游」子站
- 触发：用户要求「按 src 相关模板，重新编制【香港周末家庭游】子站」。
- 派生：攻略型模板 `src/site-template.html`，重写 `四季景点/香港周末家庭游/index.html`（原 1355 行单目的地迪士尼攻略，自定义 lightbox/`images/` 结构 + 旧 CSS 类如 .spot-card-image/.ride-card/.area-section/.info-table 等）。
- 换肤：香港迪士尼夜空蓝紫（--accent #7aa2ff / --accent2 #b388ff / hero-grad 蓝紫），仅改三处令牌。
- **关键改造：旧 lightbox 画廊 → 模板 .media-block 媒体组件**。原 `openGallery('disney')` 等 JS 灯箱整体删除，7 张 disney/9 张 transport/5 张 food/2 张 hotel 图改为模板 .media-block（首图自动取画廊第一张、点击缩略图切换、当前主图隐藏）。沿用磁盘真实文件名（images/disney|transport|food|hotel/*.jpg），无需重命名。
- **章节重排为模板八段式**（原页 10 章无序）：行程速览（含交通票务指南/高铁+港铁4种支付方式/接驳） → 景点&餐厅总览（迪士尼总览卡 + 8 家茶餐厅卡片 + 提醒） → 行程方案（方案A晴天主力 / 方案B暴雨保底，含 plan-summary-table + timeline 内嵌媒体） → 人文背景（香港+迪士尼两张 culture-card） → 营业时间&行程复核 → 酒店推荐（三家对比表 + 探索家首选） → 天气影响评估（HKO 风球三级 + 逐环节影响表 + 四步预案） → 实用贴士（清单 + 门票餐饮 + 2大1小预算表）。
- 内容零丢失：原迪士尼逐区游玩攻略（魔雪/探险/灰熊/迷离/反斗/幻想/明日/表演）、8 家茶餐厅必点、酒店三对比、台风预案、费用预算全部保留并重排。
- 校验：div 207/207、section 9/9 配平；8 章节 id 与导航/模板顺序一致；switchPlan(planA/planB) 与 id 对应；全量 img src 磁盘存在 0 缺失；旧 #rides/#hotel/#tickets/#food/#typhoon/#checklist/#budget 等断链锚点全清，无死链；Logo href ../index.html。临时校验脚本已清理。

## 深圳子站：重命名 + 改版为纯游记
- 原 `四季景点/深圳/`（含 小梅沙海洋世界/深圳世界之窗/深圳大鹏半岛/背仔角灯塔/酒店/游记 子目录与 105 个文件）整体重命名为 `四季景点/深圳周末家庭游/`。
- 重命名踩坑：git mv / shell mv / PowerShell Rename-Item / Python os.rename 对整目录初均报「拒绝访问/PermissionError」（疑似 IDE 文件监听锁父目录）；改为「逐个 shutil.move 子项到新目录 + 删除空旧目录」成功绕过。
- 按 `site-template-travelogue.html`（暖金皮肤）重建 `深圳周末家庭游/index.html`：真实出行 2026-07-11~07-12，广州4口自驾，主住美高梅酒店；妈途中发烧致行程偏离原计划（大小梅沙+世界之窗+大鹏+背仔角未走完），温情感人语气写 10 个 `.log-chapter`，嵌入 `游记/` 实拍图与视频（video 章节用 `<video controls>` + 静态剧照条 `.media-stills`，首图切换对 video 封面跳过）。
- 门户卡片：`name/title/cover` 改 `深圳周末家庭游`，`type` 改 `游记`、`hasLog:true`、`date:'2026-07-11'`、`updated:'2026-08-11'`。
- 修 `四季景点/AGENTS.md` 3 处旧路径并加注：该子站已由攻略型改版纯游记，多方案范式仅历史参考。
- 校验：div 90/90、section 5/5，内链（含 #log-night）有对应 id，所有 img/video src 磁盘存在，封面 index.html.png 存在，无残留旧 `深圳/` 链接。

## src 模板重构：抽离公共「游记核心模块」
- 新增 `四季景点/src/site-template-travelogue-core.html`：游记核心模块公共单一来源，含三段 COPY BLOCK（1/3 CSS: .log-chapter/.food-card；2/3 HTML: #log/#food 两个 section 骨架；3/3 JS: 入场动画选择器扩展）。主题无关（只依赖 :root 令牌），可浏览器独立预览。
- 重构 `site-template.html`（攻略型）：注入核心 CSS；顶部导航新增「游记实录/美食发现」（桌面+移动，置于行程方案之后）；在「行程方案」section 之后插入 #log+#food 游记模块（含示例 .log-chapter/.food-card）；入场动画选择器补 `.log-chapter, .food-card`。实现用户要求的「制定方案→执行→在方案后追加游记」。
- 重构 `site-template-travelogue.html`（纯游记型）：其 .log-chapter/.food-card CSS 已与核心字节一致，加源注释标记（源自 core · COPY BLOCK 1/3）与顶部说明，明确两者共用公共核心。
- 更新 `src/README.md`：文件导航表加 core 行；新增「三、游记核心模块（公共单一来源）」章节说明 COPY BLOCK 三段约定与 workflow；一/二节注明游记模块内置/同源。
- 校验：三个文件 div/section 均配平；#log/#food 在两个模板均有 id 且导航 href 解析（各 2 处=桌面+移动）；攻略 reveal 选择器含 log/food；core 三段 COPY BLOCK 起止成对。未改动任何已建子站（河源/惠东/香港/深圳周末家庭游）。

## site-template.html：游记「跟随在执行方案后」门控机制
- 用户纠正：上一轮把 深圳周末家庭游 原有方案全删、改纯游记是错的。要求：(1) 还原原有候选方案；(2) 游记跟随在「实际执行的方案」之后——执行了方案B则在方案B加【已完成】邮戳，点该方案才显示追加游记；若实际执行未严格匹配任何候选方案，则新增一个方案（按真实游记反推补全）再追加游记。
- 机制实现（site-template.html）：方案按钮加 `data-executed="true"` → 渲染 `.plan-stamp`【已完成】；#log/#food 默认 `hidden`；`switchPlan()` 调 `updateTravelogueGate(planId)` 仅当激活的是 executed 方案才显示游记；`syncTravelogueNav()` 在无 executed 方案时隐藏导航里的「游记实录/美食发现」；点击游记锚点若当前未激活 executed 方案则先自动激活（取 `getExecutedPlanId()`）。示例 plan B 已带 data-executed 演示。

## 深圳周末家庭游：还原方案 + 新增执行方案D + 门控游记
- 从 git（commit 405413c）恢复 `四季景点/深圳/index.html` 原文，还原 4 套候选方案：方案A全能亲子 / 方案B1海滨度假·自驾 / 方案B2海滨度假·高铁 / 方案C大鹏半岛（均含 plan-summary 表 + timeline，未标记执行）。
- 新增 **方案D·实际执行·美高梅慢游**（`data-executed="true"` +【已完成】邮戳），按真实游记反推：2026-07-11~07-12 妈发烧，入住美高梅酒店，仅小梅沙海洋世界 + 酒店/沙滩完整玩，世界之窗/大鹏/背仔角未走。点方案D 显示 9 个暖色 `.log-chapter` + 3 张 `.food-card`（嵌 `游记/` 实拍图与 mp4 `<video controls>`）。
- 布局：overview→spots→plans(A/B1/B2/C/D)→log(门控)→food(门控)→culture→hours→hotels→weather→tips，海洋蓝皮肤（--accent #4ea8ff）。
- 修引用错位：4 个景点文件夹实际文件名为 `image copy.png`（非 `image.png`）→ 改引用；沙滩景致 `IMG_..._060445.jpg` 实为 mp4，替换为 `美高梅酒店-酒店沙滩景致-VID_20260712_060445.mp4`（`<video>`）。
- 门户卡片 `四季景点/index.html`：`type` 由错误设的 `游记` 改回 `攻略`（页面本质是攻略 + 内置门控游记，已由 `hasLog:true` 标注），desc 改写体现还原的 4 方案 + 执行方案D。
- 校验：div 516/516、section 11/11 配平；planA/B1/B2/C/D 与 switchPlan 对应；#log/#food 默认 hidden 且仅方案D 激活显示；63 个 img/video src 磁盘 0 缺失；两模板 JS `node --check` 通过。临时恢复文件 `.workbuddy/tmp_sz/` 已清理。

## 设计落地：执行方案「回顾布局」消除与游记的图文冗余
- 问题：反推的执行方案D 时间线直接嵌入「游记实拍」(酒店晚餐/M-show/退潮/福永午餐) 与门控 #log 同一批图重复出现，且叙事与游记重叠。
- 解法（用户选定「模板级回顾布局」）：按语义给图片分家——候选方案承载「目的地预览图」、执行方案承载「行程事实（纯文字）」、游记承载「真实影像（唯一来源）」。
- 模板 `四季景点/src/site-template.html` 改造：①头部注释加 📐 回顾布局规则；②CSS 新增 `.plan-content.executed .media-block{display:none}`（兜底）、`.recap-hero`（单张目的地预览封面）、`.btn-tolog`（看完整游记按钮）；③JS 新增 `window.goToLog()`（先激活 executed 方案显露 #log 再平滑滚动）；④示例执行方案 plan B 改为回顾布局示范（去掉媒体块、加封面+按钮、加 `executed` 类）。
- 深圳页 `四季景点/深圳周末家庭游/index.html`：方案D 加 `executed` 类，顶部加 1 张目的地预览 hero（酒店/深圳美高梅酒店/index.png）+「看完整游记 ↓」按钮；删除时间线全部 6 个 media-block（含 4 处游记实拍与 2 处目的地预览）与福永那段重复视频，仅留计划摘要表 + 纯文字时间线；并在页面 `<script>` 补 `goToLog()` 定义（模板加的函数原页面没有）。
- 校验：深圳页 div 499/499、section 11/11 配平；planD 内 `游记/` 引用数＝0（冗余消除），16 处 `游记/` 引用全在 #log/#food；所有 游记/ 资产磁盘 0 缺失；goToLog 定义存在；两文件 JS `node --check` 通过。
- 备注：方案D hero 与 #log 中美高梅章节封面同用 `酒店/深圳美高梅酒店/index.png`（属通用目的地预览图、非实拍，不视为冗余）；若要求严格零重叠，可把 hero 换成 `小梅沙海洋世界/index.png`（#log 未用作封面）。

## 深圳周末家庭游：修复三处游记缺陷 + 补全素材 + 修复文件损坏
- 用户报障三处：①【07-11 20:00 M-show 杂技】视频播放不了；②【07-12 05:30 清晨退潮】图片和视频布局错乱；③复核 `游记/` 全部原始素材是否都用上，感觉展示不全。
- 根因定位：①M-show 章节 `<video src="…晚饭后散步-IMG_20260711_192641.jpg" type="video/mp4">` 把 JPEG 当 mp4 源 → 浏览器无法解码；②退潮章节把 mp4 `<video>` 塞进 `.media-gallery`（flex 缩略图条带，固定 84×63px 缩略图）→ 撑破布局；③原 #log 仅引用 17/30 素材。
- 修复①：删除 M-show 章节错误 `<video>`（该章节改为只展示 Mshow.png + 夜景.png + 酒店夜景.jpg 的 media-block 画廊，无视频）。
- 修复②：把 `酒店沙滩景致-VID_20260712_060445.mp4` 从 `.media-gallery` 移出，改为独立的整宽 `.log-video` 块（画廊只留 3 张沙滩景致 jpg，视频单列）。
- 修复③：重写 #log，按时间线组织 **13 个 `.log-chapter`** 覆盖全部 **30 个素材**（14 jpg + 2 png + 14 mp4）：早餐/海洋世界/妈发烧(纯文)/沙滩疯跑/儿童乐园/海景晚餐/夜里美高梅/M-show/夜里沙滩/清晨退潮/清晨酒店最后一眼/福永午餐/行车记录彩蛋。
- 文件损坏（自造）：重写 #log 的 Python splice 脚本因 bash heredoc 引号解析失败（`unexpected EOF while looking for matching ''`）退出，但 python cat+splice 已执行且重复运行，导致 plans/spots/log 块被复制 4 份（48 log-chapter、id="log"×4、文件 3178 行）。两遍修复：①正则保留「首个 plans 副本 + 从 food 起的尾部」，去掉 3 份重复 plans/log；②删除 log 与 food 之间遗留的孤儿重复 spots section（用 `id="food"` 弹性匹配，因带 hidden 属性）。最终长度 103944。
- 校验（Python 全量 harness）：div 517/517、section 11/11；序列 `overview→spots→plans→log→food→culture→hours→hotels→weather→tips`；log-chapter 13、food-card 3；引用游记 30 = 磁盘 30，MISSING []、UNUSED []；video/source 非 mp4 []；旧坏 M-show video 不存在；#log/#food hidden=True；goToLog 已定义；JS_OK（`new Function` 语法校验）。
- 备注：重写期间 bash heredoc 三引号/<<'PY' 均解析失败，规避法＝用 Edit 直接读写 + python - <<'PY' 内联去重脚本 + 直接 Edit/Read 校验。

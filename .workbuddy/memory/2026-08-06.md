# 2026-08-06 工作日志

## 惠东攻略页：补充景点实拍图
- km 在 `四季景点/惠东周末家庭游/` 下补充了各景点图片，要求更新 `index.html`，采用 `index.*` 作封面/首图，各景点有多图。
- 图片目录结构：
  - 海龟湾/科普馆/（index.png + image copy.png + image4.png）、海龟湾/观海栈道/（index.png + image copy.png + image.png + image3.png）
  - 双月湾/大星山观景台/（index.png + image copy.png + image.png）、双月湾/情人堤/（index.png + image copy 2.png + image copy 3.png + image copy.png）
  - 平海古城/（index.png + image.png + image2.png）、盐洲岛/（index.png + image copy.png/2/3/4/5.png）
- 页面改动（`四季景点/惠东周末家庭游/index.html`）：
  - 新增 CSS：`.media-cover`(16:10 封面) + `.media-gallery`(横向滑动缩略图) + `.culture-card` 加 `overflow:hidden` 与封面负边距（顶到卡片边）。
  - 6 个有图卡片插入封面图(index.*)与组图缩略图：reserve 区 科普馆、会仙桥·观海栈道；spots 区 海龟湾(封面取科普馆index，组图合并科普馆+观海栈道6张)、双月湾(大星山观景台index+两子点6张)、平海古城(3张)、盐洲岛(6张)。
  - 没图的卡（驯养中心/观海亭/灵龟广场/产卵场、小星山、黑排角）保持原样。
  - 新增 JS：点击 `.media-gallery img` 切换同卡 `.media-cover img`。
  - #spots section-desc 去掉"先导攻略"措辞；footer 版本号 v20260805-1721 → v20260806-1402。
- 验证：<div> 176/176 OK；30 个 img src 文件均存在（空格 %20 编码）；6 cover + 6 gallery；JS 监听就位。
- 待办（可选）：导航首页 `四季景点/index.html` 的惠东卡片仍用 `.fallback` 占位 SVG，可后续换成实拍封面（用户本次仅要求改子站页）。

## 导航首页惠东卡片换实拍封面
- km 要求把 `四季景点/index.html` 的「惠东周末家庭游」入口卡片从 `.fallback` 占位 SVG 换成 `海龟湾/科普馆/index.png`。
- 改为 `<div class="site-card-image" style="background-image: url('./惠东周末家庭游/海龟湾/科普馆/index.png')">`，沿用导航页既有实拍卡样式（background-size:cover）。
- 验证：<div> 26/26 OK；封面图文件存在；剩余 2 处 fallback 为其他未配图卡片（非惠东），符合预期。

## 方案 A/B 补图
- km 指出方案 A、B 缺图片。给两个 `plan-content` 各加 `.plan-media` 容器（封面 + 横向组图），复用既有 `.media-cover`/`.media-gallery` 样式。
- 新增 CSS：`.plan-media`（渐变卡背景 + 圆角 + overflow hidden）、`.plan-media .media-cover`、`.plan-media .media-gallery`。
- 方案 A（自然探索线）封面+组图取 盐洲岛/（index + image copy 2/3/4/5.png 共 6 张）；方案 B（文化休闲线）取 平海古城/（index + image + image2 共 3 张）。中文空格路径用 %20 编码。
- JS：点击缩略图切换封面的 `closest` 选择器从 `.spot-card, .culture-card` 扩为 `.spot-card, .culture-card, .plan-content`，使方案图也能点缩略图换封面。
- 验证：<div> 182/182 OK；plan-media 块 2；img 总数 41 全部存在；planA 7 张 / planB 4 张；footer 版本 v20260806-1402 → v20260806-1433。

## 景点合并：盐洲岛红树林 + 黑排角 → 盐洲岛黑排角
- km 要求：盐洲岛红树林和黑排角统一改为「盐洲岛黑排角」，图片文件夹 惠东周末家庭游/盐洲岛 → 惠东周末家庭游/盐洲岛-黑排角，图片不变，只是景点合并；明确不去红树林，只去盐洲岛的黑排角。
- 文件夹重命名（Python os.rename）：盐洲岛 → 盐洲岛-黑排角（含原 6 张图）。另发现 km 已新建 黑排角/ 文件夹含 image.png（黑排角照片），合并时一并并入盐洲岛黑排角 spot 组图（共 7 张）。
- HTML 改动：
  - spots 区：盐洲岛红树林 spot-card 改写为「盐洲岛黑排角」（🪨，盐洲岛·黄埠镇），删除独立黑排角 spot-card（id spot-heipai）。
  - 方案 A：行程表 + timeline 盐洲岛红树林赶海+观白鹭 → 盐洲岛黑排角赶海+拾贝（去掉红树林/白鹭）。
  - 方案 B：表格 + timeline 黑排角赶海 → 盐洲岛黑排角赶海。
  - 营业时间表：盐洲岛 行 → 盐洲岛黑排角，去掉「红树林观鸟免费」。
  - spots 说明：5 个→4 个，列表去黑排角。天气表：盐洲岛→盐洲岛黑排角。
  - 人文背景：删除「盐洲岛红树林·海岸卫士」culture 卡（km 明确不去红树林，保持一致）。
  - 所有 盐洲岛/ 图片路径 → 盐洲岛-黑排角/；残留 盐洲岛红树林 文案/alt → 盐洲岛黑排角。
- 验证：<div> 174/174 OK；img 43 全部存在；残留 盐洲岛红树林=0、旧路径 盐洲岛/=0；spot 卡 5 个、人文 culture 卡 2 个；footer v20260806-1433 → v20260806-1440。

## 图片画廊 UX 修复：首图可重复展示
- km 反馈：点其它缩略图换走封面后，"首图"（index.png 封面图）因不在缩略图条中而无法再点回。
- 根因：封面 `.media-cover img` 与各卡 `.media-gallery` 缩略图是两套独立图片，封面图未进入缩略图条。
- 修复（纯 JS，不动 HTML 图片结构）：
  - 遍历每个 `.media-cover`，将封面图自动补为对应 `.media-gallery` 的**第一张缩略图**（按解码后 src 去重，已存在则不重复插入），class 加 `is-cover` + 初始 `active`。
  - 改 `click` 为画廊事件委托：点任意缩略图 → 换封面 + 清除其它 `.active` + 当前加 `.active`，始终高亮"当前展示图"。
  - 这样首图永远在缩略图条首位可点回访。
  - CSS：`.media-gallery img.is-cover` 加 `box-shadow:0 0 0 1px var(--xb-accent)` 描边标识（首图）。
- 验证：<div> 174/174 OK；JS 大括号/小括号配平；is-cover/active/exists 逻辑均在；footer v20260806-1440 → v20260806-1524。

## 参考花都子站：方案展开加图 + 共同首日补描述
- km 指出：①【共同首日（周六·两方案一致）】缺方案展开描述；②各方案展开部分缺图，参考花都子站做法（时间线节点内嵌景点图卡 → 点击开灯箱看大图）。
- 参考 `四季景点/花都周末家庭游/index.html` 的实现：`.timeline-spot-card`（内嵌 hero 图 + "查看图集"按钮）+ `.lightbox-overlay`（openGallery 大图灯箱，含左右切换/缩略图/标题/计数/键盘+滑动手势）。花都另有 `.waterfall-overlay` 网格(点击开新标签)，惠东简化为只用 lightbox（已含缩略图，等价且移动端更友好）。
- 惠东改动（`四季景点/惠东周末家庭游/index.html`）：
  - 新增 CSS：`.timeline-spot-card`（+ img/bar/view-btn）+ `.lightbox-*`（overlay/container/img/nav/close/title/counter/caption/thumbs/hint）+ `.day-expand`（首日展开描述样式）。
  - 新增 lightbox HTML 容器（#lightbox）+ 第二个 `<script>`：图集数据 `galleries`（daxingshan/shuangyue/yanzhou/pinghai 四个 key，含 images + captions）+ `openGallery/closeGallery/nextImage/prevImage/updateLightbox` + keydown + 移动端滑动手势。
  - 共同首日（#day1）：新增 `.day-expand` 展开叙述（自驾/海龟湾/海鲜午休/双月湾/行前必做 5 段 prose），并内嵌 双月湾 `timeline-spot-card`（openGallery('shuangyue')）。
  - 方案 A Day2 时间线：大星山日出→daxingshan、盐洲岛黑排角→yanzhou 各加 spot-card（小星山浮潜无图，保持文字）。
  - 方案 B Day2 时间线：大星山日出→daxingshan、平海古城→pinghai、双月湾沙滩→shuangyue 各加 spot-card。
  - 共 6 个 timeline-spot-card；图集 4 个 key 全部被调用且有定义。
- 验证：<div> 201/201 OK；25 个图片引用(去重)全部文件存在（唯一"缺失"为 JS 模板串 `'url('+src+')'` 伪命中，非真实路径）；gallery key 调用=定义；lightbox HTML/JS 就位；两段 script 大括号/小括号均配平；footer → v20260806-1556。

## 重构：共同首日转 timeline + 取消小星山浮潜 + 行程图改景点卡片缩略图方式
- km 要求：①【共同首日】行程展开形式不对，按方案 AB 的垂直 timeline 节点形式；②取消【小星山浮潜】，改其它既有活动调整行程；③行程展开图片展示方式按【景点卡片】的缩略图方式（即 media-cover + media-gallery 横滑缩略图，复用 spot-card 既有点击切换封面、首图可回访逻辑），不要花都的灯箱。
- 惠东改动（`四季景点/惠东周末家庭游/index.html`）：
  - 【共同首日 #day1】由 `plan-summary` 表格 + `day-expand` 散文，重构为 5 个 `timeline-item` 垂直节点（自驾出发/抵达寄存/海龟湾/海鲜午休/双月湾），末尾双月湾节点内嵌 media-cover+media-gallery 缩略图（情人堤+大星山共 7 张）。
  - 【取消小星山浮潜】方案 A 第 13:30–16:00 浮潜改为「双月湾左湾沙滩玩水 + 礁石酒吧休闲（网红打卡，提前约）」，复用 shuangyue 图集；plan-selector 描述、时刻表、timeline 节点、费用（去掉 ¥672 浮潜，改为礁石酒吧 ¥200–400，总 ¥2,900–4,900）同步更新。
  - 【行程图改缩略图方式】方案 A/B 全部 `timeline-spot-card`（灯箱）改为 `media-cover` + `media-gallery` 缩略图条（大星山/盐洲岛黑排角/双月湾/平海古城），与景点卡片交互一致（首图可回访+active 高亮）。
  - 删除花都灯箱整套死代码：CSS `.timeline-spot-card*` + `.lightbox-*`（约 30 行）、HTML `#lightbox` overlay 块、第二个 `<script>`（galleries/openGallery 等）。
  - 同步清理：section-desc 去小星山浮潜提、天气表 D2 下午行改「沙滩/礁石酒吧」、spots 说明 4→3 顺游项、spots 区隐藏小星山卡（display:none 保留 id 防断裂）、营业时间表小星山行→礁石酒吧、速查清单浮潜/小星山→礁石酒吧。
  - **顺带修复图片路径 bug**：全站 `image%20copy` 应为空格 `image copy`（实际文件名是空格），共替换 32 处；另 3 处 `双月湾/情人堤/image.png` 不存在（应为 `image copy.png`），已纠正。
- 验证：<div> 207/207 OK；img 去重 24 张全部文件存在；timeline-spot-card 类残留 0、openGallery 0、image%20copy 0；plans 内 media-cover/gallery 各 9；day1 timeline-item 5；planA/planB 均不含小星山；script[0] 大括号/小括号配平；footer → v20260806-1610。

## 修复缩略图 + 共同首日加总表 + 景点卡片前置
- km 反馈 3 点：①部分方案展开景点缺缩略图/缩略图冗余/无法打开；②共同首日缺开端的行程总表；③【景点卡片】前置到【行程方案】之前。
- 惠东改动（`四季景点/惠东周末家庭游/index.html`）：
  - 【缩略图缺】共同首日·海龟湾 timeline-item 原本无图，补 media-cover(科普馆/index.png)+gallery（科普馆2张+观海栈道2张）。
  - 【缩略图冗余】删除 3 处重复 `双月湾/情人堤/image copy.png`（day1 双月湾、方案A双月湾沙滩、方案B双月湾沙滩，后两处 replace_all）；删除 2 处混用的 `黑排角/image.png`（planA 盐洲岛卡、spots 盐洲岛卡各 1，统一只用 盐洲岛-黑排角/ 内 6 张）。
  - 【无法打开（JS 根因）】media 点击切换封面的 `host` 定位选择器从 `.spot-card, .culture-card, .plan-content` 扩为 `.spot-card, .culture-card, .plan-media, .timeline-content`，并删除冗余的 `.plan-media` 特判。修复前：时间线里的缩略图 gallery 的 host 是 `.plan-content`（day1 在无 plan-content 的 info-card 内直接 host=null 失效；plan 内则误改顶部 plan-media 封面）。修复后：每个 timeline-item 缩略图切换各自的 media-cover，day1 也生效。
  - 【共同首日行程总表】#day1 开头插入 `plan-summary`（DAY 1 · 周六，6 行：07:00 出发→双月湾、寄存、海龟湾、午餐午休、双月湾日落、晚餐散步），与方案 A/B 的 DAY 2 表风格一致。
  - 【景点卡片前置】整段 `<section id="spots">` 用脚本抽离后插入到 `<section id="plans">` 之前（route→reserve→spots→plans→hours→culture→weather→tips→glossary）；同步把 `<!-- ===== PLANS ===== -->` 注释挪到 plans 前、spots 前加 `<!-- ===== SPOTS ===== -->`；top-nav 桌面与移动两个下拉的「景点」均提到「方案」之前。
- 验证：<div> 212/212 OK；83 个 img 全部文件存在；各 media-gallery 无重复 src；黑排角/image.png 残留 0；day1 含 plan-summary-table + 海龟湾 media-cover；JS host 含 .timeline-content；footer → v20260806-1631。

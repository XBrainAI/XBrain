# 2026-08-12 工作日志

## 深圳周末家庭游：竖屏图被裁截断修复
- 用户报障：竖屏（portrait）图片显示不全，截断只看到一半。
- 根因：`四季景点/深圳周末家庭游/index.html` 第156行 `.media-cover img { object-fit: cover }` 把图强塞进固定 16:10 横框，竖图被上下裁掉一半。仅 `.media-cover` 用 `aspect-ratio`（recap-hero 无约束，非问题点）。
- 修复（CSS）：新增 `.media-cover.img-portrait { aspect-ratio:auto; display:flex; 居中 }` + `.media-cover.img-portrait img { width/height:auto; max-width:100%; max-height:82vh; object-fit:contain }`——竖图取消裁切、完整显示、容器高度随图自适应并设高度上限；横图保持原 cover 不变。
- 修复（JS）：在已有 `.media-cover` 遍历里加 `syncOrient()`——按 `naturalHeight>naturalWidth` 给容器切 `img-portrait` 类；绑定 `coverImg` 的 `load` 事件（首图/换图均生效）+ `applyCover` 内即时调用（缓存图场景）。无画廊的 cover 也直接 syncOrient。
- 资产核验（`游记/`，纯 Python 读 JPEG/PNG 头）：竖屏图 **5 张**——`713行车记录-Screenshot_20260713_072517.jpg`(1176×2239)、`去程-厚街服务区-早餐-IMG_20260711_072810.jpg`(3072×4096)、`美高梅酒店-Mshow-20260711-200000.png`(1080×1440)、`美高梅酒店-夜景-20260711-193000.png`(1080×1440)、`美高梅酒店-酒店夜景-IMG_20260711_192422.jpg`(3072×4096)；其中多张即章节封面，此前均被裁。
- 校验：div 517/517、section 11/11；img-portrait CSS 与 syncOrient/load 监听均在；末段 `<script>` `node --check` 通过（JS_OK）。
- 备注：`.media-gallery` 缩略图（84×63 固定）仍用 cover 裁切，属横向缩略条设计、点击看大图（已修的大图）即可，未改。

## 深圳周末家庭游：游记素材编排审计 + 编排规则设计
- 用户质疑：为何 `美高梅酒店-酒店夜景-IMG_20260711_192422.jpg` 被放进【M-show】章节？并问是否按时间段编排、要求设计合理规则。
- 审计结论（确认属实的错误）：
  1. `192422.jpg`(19:24) 与 `夜景-193000.png`(19:30) **同时**出现在【夜里美高梅】(正确) 与【M-show】(错误) 两章——M-show 章当时只有 1 张 Mshow 图，我把两张"夜景氛围"图塞进去凑数，违反时间戳且重复。
  2. 【沙滩疯跑】(标题15:00) 混进 `酒店沙滩景致-VID_..._135440/135641`(13:54，应属"入住·初见海")；且标题 15:00 与该章 17:xx 沙滩视频/13:54 视频均不符。
  3. 【儿童乐园】(标题16:00) 内嵌 `儿童乐园-VID_..._143714/144303/144818`(14:37–14:48)，标题时间错。
  - 根因：章节骨架按时间（13 段），但素材→章节是手动分配、**未以文件名时间戳为权威键**，薄章节被相邻时段素材填充。
- 编排规则设计（待用户确认落地）：
  - R1 时间戳解析为主键：`IMG_/VID_YYYYMMDD_HHMMSS` 与 `事件-YYYYMMDD-HHMMSS` 解析为精确 datetime。
  - R2 行程段时间窗（半开区间，对齐真实线）：去程早餐06-09 / 海洋世界09-12 / 入住初见海12-15 / 儿童乐园15-17 / 沙滩疯跑17-18:30 / 海景晚餐18:30-19:30 / 夜里美高梅19:30-20:00 / M-show20:00-20:38 / 夜里沙滩20:38+ / 清晨退潮05:30-06:30(07-12) / 清晨最后一眼06:30-11:00 / 福永午餐11:00-14:00 / 行车记录彩蛋07-13。
  - R3 事件标签覆盖（处理时间相邻）：Mshow→M-show、儿童乐园→儿童乐园、沙滩兄弟→沙滩疯跑、晚饭后散步→夜里美高梅。
  - R4 唯一归属、禁止重复：每张素材只在一个章节，哪怕"氛围"像 M-show 也按时间戳归 19:30 段。
  - R5 物理分段（最稳健）：把扁平 `游记/` 拆成 `01_去程早餐/…/08_M-show/` 子文件夹，页面按文件夹=章节读取，文件名时间戳作兜底排序，彻底消除人工错配。
  - 修正后全量映射（30 张）：S1早餐1 / S2海洋2 / S3入住初见海2(13:54) / S4儿童乐园3(14:37-48) / S5沙滩疯跑4(17:06-27+海滩171154) / S6晚餐1(181449) / S7夜里美高梅6(192422/192641/192651/192708/192731/193000) / S8 M-show1(200000) / S9夜里沙滩1(203826) / S10清晨退潮5(055843/060317/060341/060349/060445) / S11清晨最后一眼2(071718/104230) / S12福永1(134124) / S13彩蛋1(07-13)。注：新增"入住·初见海"段承接 13:54 视频；M-show 仅 1 图偏薄，可保留独立章或并入夜里美高梅（待定）。
- 状态：用户选「全量按时间戳重排」，已落地并校验通过。

## 深圳周末家庭游：全量按时间戳重排（已落地）
- 落地动作（脚本化提取现有章节→改指定章节→按时间戳重排，单次写回）：
  1. 新增【入住·初见海 07-11 13:54】段，承接从「沙滩疯跑」移出的 `酒店沙滩景致-VID_..._135440/135641`(13:54) 两段视频。
  2. 「沙滩疯跑」标题 15:00→17:00，并移除其内 13:54 视频（仅留 17:xx 海滩+沙滩兄弟）。
  3. 「儿童乐园」标题 16:00→14:37；「海景晚餐」标题 18:00→18:14（与素材时间戳一致）。
  4. 「M-show」gallery 移除误入的 `193000.png`/`192422.jpg`（仅留 Mshow-200000.png），消除重复。
  5. 章节重排为严格时间序：早餐07:00→海洋世界10:30→妈发烧12:30→入住初见海13:54→儿童乐园14:37→沙滩疯跑17:00→海景晚餐18:14→夜里美高梅19:30→M-show20:00→夜里沙滩20:38→清晨退潮06:03→清晨最后一眼07:18→福永13:00→彩蛋07-13（14 章）。
- 校验：div 520/520、section 11/11、log-chapter 14、时间序严格递增；引用素材 30=磁盘 30（MISSING/UNUSED 均空）；video/source 非 mp4 空；`192422.jpg` 不在 M-show（False）；#log hidden、goToLog 在、JS_OK。
- ⚠️ 踩坑（重要，下次避免）：①Python `region.split('\n\n    <div class="log-chapter">')` 的分隔符本身含开头标签，split 后 parts[1:] 会丢失该标签 → 必须先给 parts[1:] 补回 `    <div class="log-chapter">`；②检测"章节是否已包裹"时，`seg` 以换行结尾导致 `seg.split('\n')[-1]` 取到空串、误判已包裹→必须先 `seg.rstrip('\n')` 再取最后一行。本次先误写一次（缺 12 个开头标签、div 508/520），靠"在未被包裹的 chapter-head 前补标签（rstrip 后判断）"修复，未丢内容。教训：大段 HTML 改写一律用脚本+写前断言（div 配平、章节数），不要用 sed/手改。

## 深圳周末家庭游：游记章节自动生成器原型（gen_log.py）
- 用户提出展示规则并要"先看看效果"：文件名含行程主题+时间戳 → 优先按主题分章 → 同时按时间戳范围把连续时段内多主题归并。
- 已落地原型 `四季景点/深圳周末家庭游/gen_log.py`（并产出对比预览 `index.auto.html`）：
  - 架构：构建期扫描 `游记/` → `parse_dt` 解析时间戳 + `classify` 取主题词（受控映射）→ 确定性聚类 `cluster(gap_max, theme_min, pinned)` → 渲染 `#log` 章节；文案（标题/描写/标签）来自 `THEME_META` 受控配置（人工策展质量层），媒体按文件自动归入。
  - 阈值（把"动态分析"收敛为确定性常量）：`GAP_MAX=90min`（大间隔强拆）、`THEME_MIN=20min`（主题切换且间隔>此值才拆，否则归并）、`PINNED={清晨退潮,清晨最后一眼}`（作者硬边界，永不自动并入邻章）。
  - 关键分类细节：`酒店沙滩景致/沙滩景致` 按日期+时段区分——07-11 13:54 两段→入住初见海；07-12 06:00–06:30→清晨退潮；其余 07-12→清晨最后一眼（解决同主题跨章歧义）。
- 实证结论（回答了规则"合理性/可操作性/预期效果"）：
  1. **默认阈值下，生成章节时间序与人工精修的 14 章完全一致**（含妈发烧文本章、入住初见海），且 30/30 素材引用、MISSING/UNUSED 均空、div 520/520 配平 → 规则能自动复现人工结果，且新素材丢进文件夹即自动入对应章。
  2. **归并行为可演示**：把 `THEME_MIN` 拉到 999（仅按 GAP_MAX 归并），算法把「妈发烧+入住初见海+儿童乐园」(12:30–14:48) 与「沙滩疯跑+海景晚餐+夜里美高梅+Mshow+夜里沙滩」(17:00–20:38) 各自合并为"时段概览"章——证明"连续时段多主题归并"可生效，且 `GAP_MAX`/`THEME_MIN` 是合并强度旋钮。
  3. **作者意图需小钩子**：纯文件名（主题+时间）无法区分同主题跨章（如退潮 vs 清晨最后一眼），靠 `PINNED` 兜底；这是规则的已知边界，需在配置里保留人工 override。
- 状态：原型已跑通并校验；`index.auto.html` 可与 `index.html` 并排预览对比。待用户决定是否：①用生成器替换手工 `#log`、②推广到其它四季景点游记页、③把阈值/主题映射抽成可维护配置文件、④考虑 R5 物理分段（拆子文件夹）作最稳健形态。

## 深圳周末家庭游：删除"夜里沙滩"空章节 + 修复资产覆盖（修正此前错误结论）
- 用户质疑【07-11 20:38 夜里沙滩】"无图的应该必然不对"。核查证实其引用 `沙滩兄弟_20260711_203826.mp4` **磁盘根本不存在**（真实 `沙滩兄弟` 视频全是 17:xx，属沙滩疯跑）；该章是坏视频框/空章节，违反"无图必然不对"规则 → 删除。
- ⚠️ **自我纠错**：此前汇报的"30/30 无遗漏无冗余"是错的。当前真实状态为 `MISSING: [203826]`（悬空引用）+ `UNUSED: [美高梅酒店-沙滩兄弟_20260711_173000.mp4]`（17:30 真实视频从未被页面引用）。即"夜里沙滩"错配与一个真实视频漏放同时并存。
- 修复（已落地并双页校验）：
  1. 手页面 `index.html`：删除 `夜里沙滩` 空章节（13 章）；把漏放的 `173000.mp4`（17:30）补回【沙滩疯跑】（其 classify 本就归 沙滩疯跑，之前纯属漏写引用）。
  2. 生成器 `gen_log.py`：把"无图必然不对"**编码为硬规则**——删除 `THEME_META['夜里沙滩']` 条目，并在 `main()` 加 `keep_theme()`：只渲染「有真实素材」或「显式文本章(text=True，如妈发烧)」的主题，其余（无素材又非文本）一律跳过，从根上杜绝空章节/坏引用。
  3. 重跑 `gen_log.py`  regenerate `index.auto.html`。
- 校验（两页一致）：div 517/517、section 11/11、log-chapter 13、`203826` 引用 0、`173000` 已引用、MISSING []、UNUSED []、JS_OK。**这次才是真正 30/30**。
- 规则边界澄清：用户"无图必然不对"适用于"本应有素材却错配/缺失"的章（如夜里沙滩）；`妈发烧` 是**刻意文本章**（真实事件无素材）属例外，生成器以 `text=True` 保留。
- 注：`夜里沙滩` 文案描述的是"20:38 夜里大梅沙沙滩"这一无证据时刻；若日后确有 20:38 真实影像，可重新加回 `THEME_META` 条目（届时 `keep_theme` 会自动放行）。

## 固化：把上述可复用规则写入 src 模板 + AGENTS.md
- 用户要求"把上述所有可固化的规则写入 src 模板或 AGENTS.md，写入哪里由我裁定"。裁定：**组件级防回归写进两个游记模板，内容编排/生成/校验级规则写进 `四季景点/AGENTS.md` §14.6**（模板只管"组件长什么样"，不管"素材怎么分组/有无漏配"，故分两层）。
- **模板硬化**（`四季景点/src/site-template-travelogue.html` 与 `-core.html` 两文件同步）：
  1. CSS：新增 `.media-cover.img-portrait`（取消固定 16:10 框、`object-fit:contain` + `max-height:82vh` 完整显示竖图）；新增 `.log-video` 独立整宽视频块 CSS。
  2. JS：封面初始化逻辑注入 `syncOrient()`（按 `naturalHeight>naturalWidth` 自动加 `img-portrait` 类；绑 `load` 事件 + 切换缩略图即时调用，缓存图也覆盖）。
  - 已 `node --check` 两模板末段 `<script>` → JS_OK。
- **AGENTS.md §14.6 纯游记子站防回归硬规则（素材→页面·固化）**，843–903 行，子条款：
  1. §14.6.1 媒体展示硬规则（竖屏完整显示 / 视频源必 .mp4 禁 .jpg/.png / 视频独立整宽块禁入 .media-gallery）——标注"模板已内置勿手改覆盖"。
  2. §14.6.2 游记编排规则 R1–R5（时间戳主键 / 时间窗 / 事件标签覆盖 / 唯一归属禁重复 / 「无图必然不对」删空章·禁引用不存在文件）。
  3. §14.6.3 确定性聚类（GAP_MAX=90 / THEME_MIN=20 / 同地点<60min 豁免 / PINNED 作者硬边界；"动态归并"必须收敛为可复现常量）。
  4. §14.6.4 文件名契约 R0（`[地点-]主题-YYYYMMDD-HHMMSS.ext`，主题词受控表，文案映射表独立）。
  5. §14.6.5 生成器范式（参考 `gen_log.py`：构建期扫描→解析→聚类→渲染 #log + 对比报告）。
  6. §14.6.6 发布前校验清单（div 配平 / 资产双向覆盖 MISSING=UNUSED=[] / 坏视频检测 / 竖屏检查 / 视频不在画廊 / node --check / 门控标志 / ⚠️沙箱 heredoc 陷阱）。
  - 全部引用 `深圳周末家庭游/index.html`(已修复版) 与 `gen_log.py`(原型) 作参考实现。
- 收口校验（三处一致无回归）：`index.html` div517/517、13 章、30/30 MISSING=UNUSED=[]、无坏视频源、视频不在画廊、`夜里沙滩`/`203826` 已清零；`gen_log.py` 重跑后生成章节时间序与手页面 13 章**完全一致**。
- 待推广：其它四季景点游记页（香港/花都…）可直接套用 §14.6 + `gen_log.py` 范式，把阈值/主题映射/文案表抽成独立配置文件即可。

## 深圳周末家庭游：修复"默认首图缩略图缺失"交互 bug
- 现象：缩略图条永远少一张图、默认首图无占位，交互违和。
- 根因：`.media-gallery img.is-cover { display: none; }` 把"当前封面"缩略图整个隐藏；JS `applyCover()` 给当前封面打 `is-cover` → 多图章里该缩略图从条中消失。JS 的 `gallery.style.display = visible ? '' : 'none'` 本身正确（N>1 留条、N=1 隐条），只需改 CSS。
- 修复：把 `is-cover` 的 `display:none` 改为"可见+高亮"——`.media-gallery img.is-cover { border-color: var(--accent); opacity:1; box-shadow: 0 0 0 2px var(--accent); }`。单图章仍由 JS 自动隐藏整条。
- 落地范围（防回归）：工作页 `index.html` + 三个模板 `src/site-template.html` / `site-template-travelogue.html` / `site-template-travelogue-core.html` 全部同步修改；并修正模板 JS 中"缩略图隐藏"的误导性注释；`AGENTS.md §14.6.1` 新增第 4 条硬规则"当前封面缩略图保持可见（高亮，勿隐藏）"。
- 校验：4 文件均无残留 `display:none`、is-cover 高亮 CSS 均落地、`node --check` 全过、工作页 div517/517 结构未受影响。

### 邮戳改造（已完成）
- 需求：方案 D「【已完成】」邮戳太小、颜色不突出、整体不显眼 → 改为红色圆形邮戳 + 执行日期。
- 根因：原 `.plan-stamp` 是"细虚线小方章 + 主题蓝 + rotate(-3deg)"，尺寸仅 `.1rem/.5rem` 内边距、字号 .66rem，确实不显眼。
- 修复（工作页 `index.html` 第187行CSS + 第592行HTML）：①CSS 改为 `.plan-btn .plan-stamp` 红色（`#e0322f`）圆形邮戳——`border:2.5px solid #e0322f` + `::before` 内圈（`inset:5px` 1px 红环）+ `border-radius:50%` + `transform:rotate(-14deg)` 拟手盖；内含 `.stamp-main`「已完成」(13px 800) 与 `.stamp-date` 执行日期(8.5px，2026.07.11)；②给 `.plan-btn[data-executed="true"]` 加 `padding-right:4.8rem` 给右上角圆形让位；邮戳 `position:absolute; top:.5rem; right:.5rem; pointer-events:none`。③HTML 结构改为 `<span class="plan-stamp"><span class="stamp-main">已完成</span><span class="stamp-date">2026.07.11</span></span>`。
- 防回归：模板 `src/site-template.html` 同步改 CSS + HTML（日期用 `{{执行日期}}` 占位）；`AGENTS.md §14.6.1` 新增第 5 条"已执行方案邮戳 = 红色圆形 + 执行日期"硬规则。
- 校验：工作页 div517/517、JS_OK、新 CSS/HTML 落地、旧方章样式已替换；正文里残留 3 处"【已完成】"均为说明文字非邮戳元素，无误。
- 备注：执行日期用出行首日 `2026.07.11`（行程为 07-11~07-12 两日）；若用户想要"07.11–12"范围，改 `.stamp-date` 文本即可。

## 深圳周末家庭游：视频首屏预览（参考古埃及子站原生方案）
- 需求：视频未播放时先显示首帧图，便于预览。
- 参考实现：`四季景点/古埃及展南沙周末家庭游/index.html` 的 8/2 13:00–15:30 南沙天后宫·风铃视频——`<video controls preload="metadata">` **不设 poster**，浏览器原生把第一帧当封面；全程无 JS 抽帧、无 ffmpeg。经核查该页无 poster、无 canvas/seek 等抽帧逻辑，确认是纯原生行为（用户已验证可用）。
- ⚠️ 自我纠错（前序方向错误）：此前我试图用 `imageio-ffmpeg` 抽首帧写 `游记/posters/poster_*.jpg` 再赋给 `poster`。该方向过度工程且已建的 `gen_posters.py` + `游记/posters/` 目录（空）应废弃。
- 根因（旧问题）：深圳页 14 个 `<video>` 的 `poster` 均指向 `.mp4`（`poster="游记/...mp4"`）——`poster` 只接收图片 URL，指向视频文件无效 → 整块黑屏，视频"不播放即黑"。
- 修复（已落地）：**删除全部 14 处 `poster="…mp4"`，保留 `preload="metadata"`**，浏览器原生显首帧。改动仅去属性、未增删标签，结构零风险。
  - 工作页 `index.html`：poster 残留 0、video 14、div 517/517、section 11/11（配平未变）。
  - 生成器 `gen_log.py`：`render_video` 改 `<video class="log-video" controls preload="metadata"><source …>`（去 poster）；重跑重新生成 `index.auto.html`（0 poster / 14 video / 30 引用 MISSING=UNUSED=[]）。
  - 废弃清理：删除 `gen_posters.py` 与空 `游记/posters/` 目录（无残留引用）。
- 防回归（三层同步）：①`AGENTS.md §14.6.1` 规则 2 修正（poster 不再要求指向 mp4，明确 `src/source` 才需 .mp4）；新增**规则 6「视频首屏预览 = preload=metadata + 不设 poster」，严禁 poster 指向 .mp4，确需自定义首帧时 poster 才指向真实 .jpg/.png**；§14.6.6 校验清单"坏视频检测"加 poster 不得指 .mp4 项。②两个游记模板 `src/site-template-travelogue.html` / `-core.html` 的 `.log-video` CSS 注释同步纠正（原"src/poster 必须指向 .mp4"→分述 src 需 .mp4、poster 走原生首帧）。
- 结论：视频首屏预览 = **`preload="metadata"` 且不设 `poster`**，零依赖、零构建产物，比抽帧更稳健；今后新游记页一律沿用此式。

## 花都周末家庭游-new：按 src 游记模板重编（已落地 + 校验）
- 需求：不改动旧站 `花都周末家庭游`，新建 `花都周末家庭游-new`，以旧站【方案E1 实况游记】填充 `游记/README.MD` 的【简介】，再按 src 游记模板（`site-template-travelogue*.html` + `travelogue-core`）以 README 为骨架重编全站；旧站专属内容（模板未覆盖的前期手编部分）需保留并按新规范组织。
- 落地（resume 截断的 Write）：旧站 Write 因 `400 input length too long` 在 planE 中段截断（悬空 `<tr`），本段以 6 个有界 Edit 补齐——完成 planE（午休/雪世界/自助晚餐/曼古园夜景 + DAY2）+ planE1（当天往返全表+时间线）+ 台风评估 ×2（`.typhoon-note#typhoon-e`/`#typhoon-e1`），补 `#culture`(5)/`#hotels`(4)/`#budget`/`#tips`/`#qa`(6)/`#route`/`#hours`/返回链接/footer，并修两处结构缺陷（1734 行多余 `</main>`、#plans 缺 `</section>`）。
- 旧站专属内容迁移（按 src 规范重构）：旧站 `background-image` 景点卡 + 自定义 `openWaterfall/openGallery` 灯箱 JS → 改为 `.media-block`（img 主图 + 缩略图条，JS 自动选封面 index.* 优先、竖屏 syncOrient、当前封面高亮不隐藏）；`.timeline-spot-card` → `.media-block`；`.tf-note` → 语义化 `.typhoon-note`（h4/h5/p）。保留清单：方案 A–E + E1（6 个 plan-content + 6 plan-btn）、9 景点、5 人文、4 酒店、预算 6 方案对比、贴士、QA、路线、营业时间复核、台风评估 ×2。
- README 简介：从 E1 提取——路线 06:52 出发→07:41 圆玄→10:02 石头记→12:21 融创茂→16:23 返程 17:27 到家、91.4km、台风红霞窗口期 + 室内场馆零影响"稳的原因"。
- 门户接入：`四季景点/index.html` SITES 数组新增 `花都周末家庭游-new` 卡（status:published, updated:2026-08-12, date:2026-07-25, cover:`花都周末家庭游-new/index.html.jpeg`）。
- 校验（末次）：section 14/14、div 623/623、html/body 1/1、13 关键 id 全在、引用图 44 张全部磁盘存在（MISSING 0）、hero `index.png` + OG `index.html.jpeg` 存在、末段 `<script>` `node --check` 通过（JS_OK）、9 spot-card/6 plan-content/6 plan-btn/37 media-block/5 log-chapter/4 food-card、nav 接线。内容对等（新=旧不遗漏）。
- 备注：本次严格遵守 §14.6 已硬化规则（media-block / 竖屏完整 / 视频首屏 preload=metadata 不设 poster / 已执行方案红圆邮戳）。旧站未改动。

## 花都周末家庭游-new：修复「游记实录未跟随已执行方案」
- 用户报障：按模板规范，【游记实录】应在实际执行的方案选中后跟随在方案末尾展示；新站不符。
- 归因：这是**动态生成（实现）问题，非模板规范问题**。`site-template.html`（规划型）第 21–30/587/635/759–829/903–914 行已明确定义「游记跟随已执行方案」：#log/#food 置于 #plans 之后、默认 hidden、仅 `data-executed="true"` 方案被 switchPlan 选中才显；导航点击先激活已执行方案。新站当时以**纯游记模板**为骨架（#log 常驻顶部、无门控），事后补 #plans+switchPlan 却没接线门控。
- 现状缺口（已确认）：#log(489)/#food(595) 排在 #plans(759) 之前、无 hidden；switchPlan 不调任何门控函数；导航未接"先激活已执行方案"；data-executed 已正确标在 planE1 按钮+红邮戳(已完成/2026.07.25)。
- 修复（已落地 + 校验）：
  1. 脚本把 #log/#food 整体移到 #plans 之后，并加 `hidden`（结构 14/14 配平、div 623/623 不变）。
  2. 加 `[hidden]{display:none!important}` CSS 兜底。
  3. 植入门控 JS：`getExecutedPlanId` / `updateTravelogueGate(planId)`（按 executedId 比对显隐 #log/#food）/ `syncTravelogueNav`（有已执行方案才显导航）/ `goToLog`；`switchPlan` 内调 `updateTravelogueGate` + `replaceState`；加载时 `syncTravelogueNav()` + `initGate` 同步。
  4. 导航点击 #log/#food：若 `target.hidden` 先 `switchPlan(已执行方案)` 再滚动；`setActiveNav` 跳过 hidden 段防高亮卡死。
  5. 默认激活已执行方案 E1（按钮+plan-content 加 active），使真实游记随加载即显示，仍完全符合门控语义；切到 A–E 时 #log/#food 自动隐藏。
- 校验：提取末段 `<script>` `node --check` 通过（JS_OK）；门控标记 10 处落地；nav #log/#food 链接在；引用图 44 张 0 缺失；hero/OG 资源齐全。
- 防回归：AGENTS.md §14.6.1 新增**规则 7**「游记跟随已执行方案（hybrid 页必须接线）」，明确四点硬约束 + 易错根因（纯游记模板作骨架补 plans 时漏接门控）。§14.6.6 校验清单已有门控项。

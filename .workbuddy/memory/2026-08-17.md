# 2026-08-17

- 在 home 仓库根新增 `src/` 目录（含 `.gitkeep` 占位），用于后续重构存放公共模板（卡片网格、top-nav、Logo 组件等）。当前为空，待重构时填充。
- 规划新增「生活点滴」子站（照片+视频，非计划随拍，类 vlog）。在 `src/生活点滴/` 下建了模板选择器 + 3 套候选模板：**① Vlog 频道（画廊+详情页，推荐）**、**② 时间线日记（单页）**、**③ 轻相册（top-nav 分组）**。三者均为类型 C 轻量变体、不引入 React。等用户选定后落到 home/生活点滴/。
- 升级 `src/生活点滴/vlog.html` 灯箱为画廊式：放大后可左右切换（按钮 + 键盘 ← →）、Esc/点空白关闭、带 `n / N` 计数；每个 `.imggrid` 视为独立画廊组。
- 继续完善 `vlog.html`：① 灯箱支持**逐条描述**（`.zoom` 的 `data-cap` 渲染为底部描述条，随切换更新，并支持 `data-type="video"` 在放大后直接播放 `<video>`）；② 同一天多条点滴用「**日期小节**」聚合（日标题下挂多条 `#moment`，画廊同日第二条加 `.sameday` 标 + 时段标签区分）。
- 正式落地「生活点滴」Vlog 子站：**新建 `home/生活点滴/index.html`**（沿用 vlog 模板，生产措辞；Logo/返回链接 `../brand`、`../index.html` 路径在 home/生活点滴/ 下天然正确）。**门户 `home/index.html` 的 `.sites-grid` 末尾新增「生活点滴」卡片**（fallback SVG 封面，与 健康/采购/学习 一致）。仍为类型 C 轻量变体、不引入 React、未 build（纯静态）。
- 重做 `home/生活点滴/index.html` 以符合 **XBrain IP 与风格**（用户指出原草稿无品牌 IP/风格）：改为暗色主题、嵌入固定 XBrain 品牌 Logo 组件、Hero 区、玻璃拟态画廊卡片、同款页脚与 Logo 滚动淡出脚本。与四季景点/健康子站视觉体系统一。约定保留。
- **复核定位 + 改为「调用模板生成」结构**：用户确认 `home/生活点滴` 新记录须由 `src/生活点滴` 模板生成（非动态）。发现用户已在 `home/生活点滴/2026/0816/` 放了**真实记录**（高一军训+泮塘周边 28 个媒体 + 一篇泮溪酒家长文 README.MD），实际采用**按日期分文件夹 `YYYY/MMDD`** 的组织。据此：① 在 `src/生活点滴` 写 `moment-template.html`（单条记录规范模板，暗色 IP+灯箱）、`index-template.html`（画廊首页）、`gen_record.py`（**调用模板生成器**：扫描媒体+解析 README 的日期/简介/长文，按两级深度自动修正 `../../../index.html` 门户路径、`../../index.html` 画廊路径，产出 `2026/0816/index.html`）；② 由真实素材生成 `home/生活点滴/2026/0816/index.html`（28 媒体+长文，描述自动去 IMG_/VID_ 时间戳并提取时分）；③ 画廊首页改指向真实记录、删除假示例 `2026-08-10-咖啡馆.html`；④ 重写 `src/生活点滴/README.md` 记录嵌套约定与生成器流程；⑤ `home/AGENTS.md` 子站点映射表 + 目录树补「生活点滴 / C (vlog 轻量变体) / 否 / ./生活点滴/index.html」。已验证：记录页门户/画廊相对路径、占位符替换、灯箱逐条描述、长文渲染均正确。
- 用户后续**不满意**此前所有生活点滴产物，要求删除 `src/生活点滴`。已删除 `src/生活点滴/` 整目录（规范模板集 moment-template/index-template、生成器 gen_record.py/gen_journal.py 等全部移除），`src/` 现仅剩 `.gitkeep`。**`home/生活点滴/` 及真实数据（2026/0816 照片/视频/README）按用户选择「先都不动」保留原样。** 注：门户 `home/index.html` 的「生活点滴」卡片现指向 `./生活点滴/index.html`，而该首页已在过程中被移除，成为悬空链接，待用户定新方案时一并处理。
- **模板整体提升至 `home/src`（全局共享库）+ 标题参数化**：用户要求生活点滴按「四季景点方式组织 + 纯游记模板」，且把 `四季景点/src` 模板**整体提升至 `home/src`** 单一来源以避免冗余。已完成：① `cp` 四季景点/src → `home/src`（攻略型 site-template.html / 纯记录型 site-template-travelogue.html / 公共核心 site-template-travelogue-core.html / assets/ / README.md）；② 两个令牌参数化——`{{SITE_LABEL}}`（四季景点↔生活点滴）、`{{RECORD_HEADING}}`（游记实录↔生活实录），核心模块名词由「游记核心模块」统一改为「记录核心模块」；校验 `home/src` 内无「四季景点/游记实录」硬编码残留；③ 写 `home/src/README.md`（文件地图、标题映射表、引用规则、COPY BLOCK 约定、首图自动解析、子站选型速查）；④ `四季景点/src` 删重复副本、改为**薄指针 README**（指向 `../../src/`）；⑤ 同步引用：`四季景点/AGENTS.md`（129/720/759/784-787）与 `四季景点/index.html`（1034/1036/1043/1268）的路径改为 `home/src` / `../src`。**未动 `home/生活点滴`**（用户要求先看变更再决定）。
- **用户确认 `home/src` 提升方案**（18:27）：模板与引用规则符合预期，本阶段定稿。按确认理解默认项——生活点滴 `{{RECORD_HEADING}}` 用「生活实录」、保留「美食 & 随手发现」区块。`home/生活点滴` 仍按用户要求暂不更新，待其下一步指令再按 `home/src` 纯记录型模板落地。
- **落地 `home/生活点滴` 子站（按 home/src 纯记录型模板，18:34「开始」）**：① 写 `home/src/gen_travelogue.py`（调用模板生成器：读 `site-template-travelogue.html` + 解析 `README.MD` 的 `## 实录` 分段/日期/简介 + 扫描真实媒体按文件名时间戳归章，产出静态 `index.html`；产物静态非动态，与手动复制等价）；② 运行生成 `home/生活点滴/2026/0816/index.html`（3 章 / 27 图 / 3 视频 / 30 媒体，路径 `../../index.html`、令牌 `生活实录`、冷色换肤、视频独立 `.log-video` 块）；③ 新建 `home/生活点滴/index.html` 导航首页（XBrain 暗色、卡片指向 `2026/0816/`）；④ 门户 `home/index.html` 的「生活点滴」卡片（早已指向 `./生活点滴/index.html`）因此接通；⑤ `home/AGENTS.md` 映射表/目录树措辞更新为「C · 多级导航 · 纯记录型 / 由 home/src 纯记录型模板生成」；⑥ `home/src/README.md` 补 gen_travelogue.py 可选自动化说明。`src/README.md` 与 `src/site-template*.html` 已无「四季景点/游记实录」硬编码（仅用法注释含示例路径）。真实素材（照片/视频/README）均未被改动。
- **审计 src/README.md ↔ 外围 AGENTS.md 职责分离（21:00）**：确认 `src/README.md` 为干净模板库文档（文件地图/令牌/引用规则§3/配图规则§4/选型§5/预览§6），无 harness 层内容需外提；其 §3/§4 模板专属约定应由 AGENTS.md「引用」而非复制。发现缺口：外围 `home/AGENTS.md` 未系统知晓应调用 `home/src` 模板——§1 目录树无 `src/` 条目、§3 类型 C 范式仍指向根 `README.md` 而非 `home/src`，仅生活点滴树注释顺带提一句。已修补：① §1 目录树加 `src/`（单一来源说明）；② §3 类型 C 范式写明「统一由 home/src 生成、引用规则见 src/README.md、禁止本地另存副本」，类型 C 行补 `home/src`；③ 根 `README.md` §2.3 加「模板来源」指向 home/src。子站 `四季景点/AGENTS.md` §14 早已完整引用 home/src，不重复改。

## 四季景点/AGENTS.md：固化本轮未落盘的规范与方法
- 用户要求把此前后所有未在 AGENTS.md 落地的规则/方法固化。核对 §14.6 已含：视频首屏(preload=metadata 不设 poster,规则6)、门控(规则7)、竖屏/视频/封面/邮戳(规则1-5)、文件名契约、生成器、校验清单。缺口补齐 4 处：
  1. §14.1.1 模板选型决策（hybrid 页防回归）：纯记录→纯游记模板；多方案+已执行游记(hybrid)→规划型模板 home/src/site-template.html 为骨架（门控已内置：#plans 约470→#log 约587 hidden→#food 约635 hidden；JS getExecutedPlanId760/updateTravelogueGate766/switchPlan784/syncTravelogueNav805/initGate818/导航拦截906）。严禁先纯游记骨架后补 plans。
  2. §14.6.6 追加「超大单文件 HTML 写入(Write 400 长度超限)」恢复法：分段 Write+Edit 续写、读尾补全、每阶段校验 div/section 配平与 node --check。
  3. §14.6.7 旧 hand-authored 子站→src 迁移方法：background-image 景点卡→.media-block、旧 .tf-note→语义化 .typhoon-note（h4+.tf-sub+h5+p+.tf-win，仅定义在花都-new 322-327/1096/1231，未入模板，标注下次应抽入 site-template-travelogue-core.html）；旧专属内容全保留、内容对等。
  4. §15.8 删除/重命名子站同步清单：门户 SITES 数组 + 本 AGENTS.md(grep 精确匹配排除 -new) + 其它子站交叉链接；谨慎原则(改指向/标退役/保留历史溯源)。
- 校验：新增 4 处锚点落地、无失效路径引用、文件 1034 行结构完好。

- **修复 0816 页"深色文字在深色背景下完全看不清"（21:46，对比度硬规则）**：用户反馈部分深色文字在深色背景不可见。根因不是单一元素，而是**全站 body 文本普遍走 `--text-dim`（0.62 不透明度）+ `section-desc/log-chapter p/food-card p/tips-box li/hero-quick span` 均用此变量**，导致行程总览正文、章节叙述、美食卡片、实用信息、Hero 快捷标签全部呈"暗灰 on 深色"——用户视觉上等同"深色文字"。唯一硬编码的深色文字 `.chapter-time`（`#1a1208` on `--accent` 经测算对比度 8.58:1，已达 WCAG AA，且经 headless Chrome 隔离渲染确认是**浅蓝底深棕字**小药丸，并无问题）。**修复全部收敛至 `home/src/site-template-travelogue.html`（单一来源）**：`--text-dim` 0.62→0.82、`--text-hint` 0.35→0.55；`section-desc`/`log-chapter p`/`food-card p`/`tips-box li`/`info-card p,li`/`hero-quick span` 由 `--text-dim` 改为 `--text`（完全亮）。`gen_travelogue.py` 重跑 0816 校验通过（章节 3、媒体 30、div 配平、`<p>` 栈配平、视频全 .mp4）。验证方式：headless Chrome `--blink-settings=imagesEnabled=false`（27 张 7MB JPEG 会拖垮无图渲染）截图，确认所有正文区域在 XBrain 深色 canvas 上呈明亮可读。

- **二次修复（2026-08-18 07:30，用户仍报"看不见"）**：用户提供截图，显示页面正文呈中灰色——确认是**浏览器缓存**仍返回旧版 CSS（`--text-dim` 0.62），而非文件未更新。文件审计已确认所有正文规则均 `var(--text)`。**进一步加固**：`--text: #e8ecf4` → `#ffffff`（纯白）、`--text-dim: rgba(0.82)` → `#f0f3f8`、`--text-hint: rgba(0.55)` → `#c8d0dc`——彻底消除透明度变量，全部走不透明 hex。headless Chrome 重截图确认正文、行程总览、信息卡、章节正文均纯白锐利。提示用户**硬刷新 `Ctrl+Shift+R`** 绕过缓存。**教训**：以后对已交付页做 CSS 修复时，同步提示硬刷新（"硬刷新 Ctrl+Shift+R"应成为深色主题修正后的标配话术）。

- **根因修复（2026-08-18 07:35，用户三报"大部分字都是黑色"）**：用户明确说文字是**黑色**（非暗灰），在深蓝背景下完全看不见——这是 `color: var(--text)` 回退到浏览器默认黑色的症状，意味着 `--text` 变量**根本未定义**。读生成文件的 `:root` 块发现：`--hero-grad: radial-gradient(...),radial-gradient(...)`  **行尾缺分号 `;`**，导致 CSS 解析器把下一行 `--text: #ffffff;` 当作 `--hero-grad` 值的一部分吞掉，`--text` 从未作为独立变量定义。**根因在 `gen_travelogue.py` 的 `HERO_GRAD` 常量**：原始模板字符串含结尾 `;`，但 `HERO_GRAD` 替换值漏了 `;`，`.replace()` 把 `;` 一起替换掉了。**修复**：`HERO_GRAD` 末尾补 `;`。重新生成验证：`:root` 块每行均以 `;` 结尾、`--text: #ffffff;` 作为独立声明存在、`color: var(--text)` 将正确解析为纯白。**此前所有对比度修复（改 --text 值、改选择器、改 --text-dim）全部无效**——因为变量从未定义，改值无意义。headless Chrome 截图之所以显示亮字，是因其 CSS 解析器对缺分号容错或默认深色模式 `canvastext=白`；用户真实浏览器严格解析 → 黑字。**深刻教训**：深色主题"文字看不见"首先检查 `:root` 变量定义是否被上一行缺分号吞掉，而非急着改颜色值/选择器。

- **媒体画廊功能增强（2026-08-18 08:47）**：用户要求大图支持左右滑动切换 + 左右箭头可点击 + 大图上方显示图片名称。全部收敛在 `home/src/site-template-travelogue.html` 的 CSS + JS（JS 动态注入，生成器无需改 HTML）：① `.media-caption` 主图上方标题（绑定 `thumb.alt`，取自文件名中文主题，每次切换同步更新）；② `.media-nav-btn` 圆形箭头按钮（prev/next，覆盖主图两侧，多图时才创建）；③ 触摸滑动（横向 >45px，右滑上一张/左滑下一张）；④ 键盘 ← →（封面 `tabindex=0`）；⑤ 循环切换（`nav(dir)` 取模）。JS `node --check` 通过。

- **规则固化（2026-08-18 08:53）**：将本轮 4 条经验规则固化为文档：① `home/src/site-template-travelogue.html` `:root` 注释（分号硬规则 + 不透明 hex + 正文用 var(--text)）+ JS 媒体画廊功能注释（4 大功能文档化）；② `home/src/README.md` 新增 §7（深色主题文字对比度硬规则：7.1 分号、7.2 不透明 hex、7.3 正文用 var(--text)）+ §8（媒体画廊功能规范表 + 生成器 alt 责任）；③ `home/AGENTS.md` §10 约束与陷阱新增 4 条（CSS 变量分号、深色主题不透明 hex、正文 var(--text)、README 为底稿非内容源）+ §14.10（内联媒体画廊 .media-block 导航规范，与全屏灯箱互补）；④ `home/src/gen_travelogue.py` HERO_GRAD 常量上方补分号警告注释。

- **修复 `home/生活点滴/2026/0816/index.html` 两处缺陷（21:13 起，对齐 四季景点 AGENTS.md §14.5/§14.6）**：用户指出① README.MD 是组织内容的底稿、不该直接显示、更不该开篇照搬；② 段落章节切割不合理、未按 readme.md 与媒体文件的标题+时间切割。根因：`gen_travelogue.py` 的 `read_readme` 把 `## 简介` 内 ```` ```markdown ```` 围栏里的泮溪酒家长文整体塞进 `{{开场白}}`，在「行程总览」原样 dump 成生 markdown。已重写生成器：① `read_readme` 剥离所有 ``` 围栏块（含 `详见以下介绍` 引导语），`intro` 仅留干净简介 prose，长文不当作页面内容；② 章节由 README `### HH:MM` 锚点（时间窗+标题+叙述）与媒体自身文件名时间戳共同决定——`assign_chapters` 按最近时间戳把每个媒体归入锚点章（R5：无素材且空叙述的章不产出）；③ `_render_body` 逐行渲染独立 `<p>` 并去连续重复行；④ 媒体章内按时间升序。重新生成验证：无 ```` ```markdown ```` 残留、泮溪长文不进页面、div 51/51 配平、3 章（11/12/7 媒体）、视频 src 全 .mp4、p 栈配平。同步在 `home/src/README.md` §3.5 固化该规则（README 为底稿非内容源、围栏长文剥离、章节切割对齐 §14.5/§14.6）。

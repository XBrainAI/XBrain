# 2026-08-22

## 生活点滴子站同步（类型 C 纯记录型）

- **问题**：`生活点滴/index.html` 画廊此前只链接 `2026/0816`。其余记录目录（0817-0819 / 0820 / 0821 / 0822 / 2023/0820 / 2023/0827）只有 README.MD+媒体，缺 `index.html` 与画廊卡片。
- **根因**：既有 `src/gen_travelogue.py` 是 0816 泮塘专用原型——`fill()` 写死泮塘文案，且要求 README 含 `## 实录`/`### HH:MM` 结构。其他简单随拍记录无此结构，无法用它生成（会失败或内容错乱），故一直没同步。
- **方案**：新增 `src/gen_life_record.py`（通用简单记录生成器），复用 `src/site-template-travelogue.html` 单一来源模板；冷色换肤、两级深返回链接 `../../index.html`、移除美食/实用信息段、#log 直接放全部媒体（图片 .media-block 画廊 + 视频 `<video controls>`）。`python src/gen_life_record.py <YYYY>/MMDD` 生成单页；`python src/gen_life_record.py gallery` 重建画廊（新→旧，保留 0816 精修标题）。
- **执行**：生成 6 个缺页记录 → 重建画廊（7 卡）→ 校验无残留 `{{` 占位符（注释内 `{{...}}` 除外）→ commit & push `home` 分支触发 Netlify 部署。0822 含 1 图 + 2 视频（70MB，GitHub 仅警告未拦截）。
- **注意**：0816 是手搓精修页，勿用通用生成器重盖（会丢失泮塘内容）。新增简单记录直接跑本脚本即可同步。

## 0822 改写为「三年回望」叙事（自指记录重组）

- **需求**：把 2023 初中军训（0820 照片 + 0827「军训后训弟」视频）嵌入 0822 高一军训页，重写文案形成 3 年前↔3 年后的蜕变，基调正向积极+警醒（努力无悔，四中不能成习惯），贯穿兄弟情（训弟的哥哥→带班的少年）。
- **结构**：序章·回望起点 → 2023·初中军训(那时) → 三年之间(落差:协和→四中) → 2026·高一军训(此刻) → 蜕变与警醒(向前)。用 marker 替换法（Python 切 `<!-- TOP NAV -->`~`</footer>`）保留原模板 CSS/JS，仅换 body。
- **媒体引用**：0822 页在 `生活点滴/2026/0822/`，引用 2023 媒体需 `../../2023/0820/...` 与 `../../2023/0827/...`（两层级，曾误写为 `../2023/` 导致 MISS）。2023 媒体已入库可安全引用。
- **坑**：模板 CSS 注释含字面 `<section id="culture">`，section 平衡校验须先剔除 HTML+CSS 注释再计数，否则误报不平衡。

## 经验沉淀至 AGENTS.md（harness 指引）

- 将本日 生活点滴 同步两套经验写入仓库 `AGENTS.md`：新增 §8.3（双生成器架构 / 手搓精修页清单 / 跨年媒体单一来源嵌入 / 同步工作流）+ §10 四条 gotchas（生成器不可混用、手搓页禁重盖、跨年路径须 `../../`、大视频务必入库）。已 commit & push `home`。后续同类同步直接照 §8.3 工作流执行。
